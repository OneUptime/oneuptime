<% if(blogPost.tags.length> 0){ %>
    <div class="flex flex-wrap gap-2">
        <% for(var i=0; i<blogPost.tags.length; i++) { %>
            <a href="/blog/tag/<%- blogPost.tags[i].replaceAll(' ','-').toLowerCase() -%>" class="group/tag">
                <span class="inline-flex items-center gap-1.5 rounded-full border border-gray-200 bg-gray-50 px-3 py-1 text-xs font-medium text-gray-700 shadow-sm hover:border-slate-300 hover:bg-slate-100 hover:text-slate-700 transition">
                    <span class="h-1.5 w-1.5 rounded-full bg-slate-500 group-hover/tag:bg-slate-600"></span>
                    <%- blogPost.tags[i] -%>
                </span>
            </a>
        <% } %>
    </div>
<% } %>