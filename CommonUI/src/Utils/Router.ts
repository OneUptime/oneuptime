import {
    Routes,
    Route as PageRoute,
    useNavigate,
    BrowserRouter,
    useLocation,
} from 'react-router-dom';

import ReactDOM from 'react-dom/client';

export default {
    Routes,
    PageRoute,
    useNavigate,
    useLocation,
    BrowserRouter,
    ReactDOM
}