enum FieldType {
    ObjectID,
    Name,
    Hostname,
    URL,
    Route,
    Number,
    Password,
    Text,
    Email,
    Date,
    LongText,
    Color,
    Port,
    HiddenText,
    Actions,
    Boolean
}

export default FieldType;
