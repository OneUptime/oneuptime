import AlertTriage from "../../Components/Alert/AlertTriage";
import PageComponentProps from "../PageComponentProps";
import React, { FunctionComponent, ReactElement } from "react";

const AlertTriagePage: FunctionComponent<
  PageComponentProps
> = (): ReactElement => {
  return <AlertTriage />;
};

export default AlertTriagePage;
