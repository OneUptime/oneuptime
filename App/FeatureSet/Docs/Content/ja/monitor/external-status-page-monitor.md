# 外部ステータスページモニター

外部ステータスページモニタリングを使用すると、サードパーティのステータスページを監視し、依存しているサービスで障害やパフォーマンス低下が発生した際にアラートを受け取ることができます。OneUptimeは定期的に外部ステータスページ（AWS、GCP、Azure、GitHubなど）を確認し、そのステータスを評価します。

## 概要

外部ステータスページモニターは、公開されているステータスページに問い合わせることで、依存しているサービスの正常性を確認します。これにより、以下のことが可能になります。

- アプリケーションが依存しているサードパーティサービスの可用性の監視
- アップストリームプロバイダーで障害が発生した際のアラート受信
- 個々のコンポーネントのステータス追跡（例：「AWS EC2 us-east-1」）
- ユーザーへの影響が出る前のパフォーマンス低下の検出
- 自社のインシデントとアップストリームプロバイダーの問題の相関分析

## サポートされるプロバイダー

OneUptimeは以下の方法によるステータスページの監視をサポートしています。

| プロバイダータイプ | 説明 |
|---|---|
| **自動**（デフォルト） | ステータスページの形式を自動検出 |
| **Atlassian Statuspage** | Atlassian Statuspage（JSON API）を利用したステータスページ |
| **RSS** | RSSフィードを提供するステータスページ |
| **Atom** | Atomフィードを提供するステータスページ |

### 自動検出

**自動** に設定すると、OneUptimeはステータスページの形式を自動的に検出しようとします。

1. まず、Atlassian Statuspage JSON API（`/api/v2/status.json` および `/api/v2/components.json`）を試みます
2. 失敗した場合、RSSまたはAtomフィードとしてページの解析を試みます
3. 最後のフォールバックとして、基本的なHTTPの到達可能性チェックを実行します

## 外部ステータスページモニターの作成

1. OneUptime ダッシュボードで **モニター** を開きます
2. **モニターの作成** をクリックします
3. モニタータイプとして **外部ステータスページ** を選択します
4. 監視するステータスページのURLを入力します
5. 必要に応じてプロバイダータイプを選択します（デフォルトは自動）
6. 必要に応じてコンポーネント名を入力して監視対象を絞り込みます
7. 必要に応じて監視条件を設定します

## 設定オプション

### ステータスページURL

監視する外部ステータスページのURLを入力します。Atlassian Statuspageを使用したサイトの場合、通常はルートURL（例：`https://status.example.com`）です。RSS/AtomフィードはフィードURLを直接入力してください。

### プロバイダータイプ

ステータスページのプロバイダータイプを選択します。OneUptimeが形式を自動検出するには **自動**（デフォルト）を使用し、形式が分かっている場合は特定のプロバイダータイプを指定します。

### コンポーネント名フィルター

ステータスページが複数のコンポーネントを報告している場合、オプションでコンポーネント名を指定して特定のコンポーネントのみを監視できます。たとえば、us-east-1のAWS EC2のみを監視する場合は、`EC2 us-east-1`（ステータスページに表示される正確なコンポーネント名）を入力します。

コンポーネント名を指定しない場合、ステータスページの全体的なステータスが監視されます。

### 詳細オプション

#### タイムアウト

ステータスページからのレスポンスを待つ最大時間（ミリ秒）。デフォルトは10000ミリ秒（10秒）。

#### リトライ回数

リクエストが失敗した場合の再試行回数。デフォルトは3回。

## 監視条件

以下の条件に基づいて、外部サービスがオンライン、パフォーマンス低下、またはオフラインと判定されるタイミングを設定できます。

- **オンラインか** — ステータスページが到達可能でステータスデータを返しているか
- **全体ステータス** — ステータスページの全体的なステータス指標（例：「operational」、「major_outage」）
- **コンポーネントステータス** — 特定のコンポーネントのステータス（コンポーネント名フィルターを使用している場合）
- **アクティブなインシデント数** — ステータスページで報告されている現在アクティブなインシデント数
- **レスポンスタイム** — ステータスページのデータを取得するまでの時間

## 主要なステータスページURL一覧

監視できる主要なサービスのステータスページURLを以下に示します。

| サービス | ステータスページURL |
|---|---|
| AWS | `https://health.aws.amazon.com/health/status` |
| Google Cloud Platform | `https://status.cloud.google.com` |
| Microsoft Azure | `https://status.azure.com` |
| GitHub | `https://www.githubstatus.com` |
| Cloudflare | `https://www.cloudflarestatus.com` |
| Datadog | `https://status.datadoghq.com` |
| PagerDuty | `https://status.pagerduty.com` |
| Twilio | `https://status.twilio.com` |
| Stripe | `https://status.stripe.com` |
| Slack | `https://status.slack.com` |
| Atlassian（Jira、Confluence） | `https://status.atlassian.com` |
| Vercel | `https://www.vercel-status.com` |
| Netlify | `https://www.netlifystatus.com` |
| DigitalOcean | `https://status.digitalocean.com` |
| Heroku | `https://status.heroku.com` |
| MongoDB Atlas | `https://status.cloud.mongodb.com` |
| Fastly | `https://status.fastly.com` |
| New Relic | `https://status.newrelic.com` |
| Sentry | `https://status.sentry.io` |
| CircleCI | `https://status.circleci.com` |

> **注意：** これらの多くはAtlassian Statuspageを使用しているため、**自動** プロバイダータイプで自動的に検出されます。

## インシデント・アラートテンプレート

外部ステータスページモニターからインシデントやアラートを作成する際に、以下のテンプレート変数を使用できます。

| 変数 | 説明 |
|---|---|
| `{{isOnline}}` | ステータスページがオンラインか（true/false） |
| `{{responseTimeInMs}}` | レスポンスタイム（ミリ秒） |
| `{{failureCause}}` | 障害の原因（存在する場合） |
| `{{overallStatus}}` | 全体ステータス指標の値 |
| `{{activeIncidentCount}}` | アクティブなインシデント数 |
| `{{componentStatuses}}` | コンポーネントステータスのJSON配列 |

## ベストプラクティス

- **自動プロバイダータイプを使用する** — 正確な形式が分かっている場合を除き、自動検出はほとんどのステータスページで適切に機能します
- **特定のコンポーネントを監視する** — 特定のサービス（特定のAWSリージョンなど）のみに依存している場合は、そのコンポーネントを監視します
- **インシデントの相関分析を設定する** — 自社モニターが問題を検出し、アップストリームのステータスページも問題を示している場合、根本原因の特定が速くなります
- **他のモニターと組み合わせる** — 包括的な可視性のために、外部ステータスページモニターと自社のAPIやウェブサイトモニターを組み合わせます
