export default {
    addSchedulesToMonitorCriterion: (
        scheduleIds: $TSFixMe,
        monitorStatus: $TSFixMe
    ) => {
        switch (monitorStatus) {
            case 'up':
        }
    },
};
