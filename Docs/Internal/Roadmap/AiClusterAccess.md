# AI cluster access — OneUptime AI with a terminal on the cluster

**Status:** shipped for Kubernetes (investigation + remediation). Hosts, Docker, Podman and the other resource kinds are the next epic and reuse every layer below except the executor.

## The problem this solves

An AI investigation could only read the telemetry the agents ship. For a Kubernetes signal that is frequently not enough: "pods stuck in Pending" is diagnosed by describing the pod and reading its events, which no metric carries. The investigation on the alert that started this work finished "without a report" for exactly that reason.

Remediation existed (the `AutoRemediationRule` + `RemediationExecution` lane: policy-checked Bash/SSH commands on opted-in Runners) but reaching a cluster took five steps: create a Runner, install it, create a Kubernetes credential, assign it, write a rule that composes commands. Customers got lost.

## What shipped

One helm flag gives OneUptime AI kubectl on a cluster; the cluster's **AI** page is the single place that says what AI may do and what is missing.

```
helm repo update
helm upgrade kubernetes-agent oneuptime/kubernetes-agent -n oneuptime-agent --reuse-values \
  --set aiAccess.enabled=true \
  [--set aiAccess.remediation.enabled=true [--set "aiAccess.remediation.namespaces={web,api}"]]
```

`helm repo update` is part of the command, not a nicety: an old local index resolves the chart the release was installed from, whose schema rejects `aiAccess.*`. Published chart versions follow the OneUptime version (`release.yml` packages the chart with `--version`/`--app-version` set to it), so customer-facing copy never names `Chart.yaml`'s own version. `kubernetes-agent` in `oneuptime-agent` is what the dashboard's install instructions use.

| Layer | Where | What it does |
| --- | --- | --- |
| Policy | `Common/Utils/AiRemediation/KubectlPolicy.ts` | Pure, three-place kubectl policy: tokenizes a one-line command into an argv (shell quoting only; kubectl is never spawned through a shell) and tiers it `Read` / `SafeWrite` / `RiskyWrite` / `Denied`. Credential/cluster-selection/raw-API/file/verbose flags, file-reading output formats (`-o jsonpath-file` / `go-template-file` / `custom-columns-file`, `--template`, and bare `-o jsonpath`/`go-template`/`custom-columns` without an inline template), exec/attach/cp/port-forward/apply/edit/replace, Secret objects in every verb (never read, changed, deleted or created: `create secret`, `create token` and `set env --resolve` included), `--all-namespaces` writes, `delete --all` and deleting namespaces/nodes/volumes/CRDs are Denied even with approval. Flags are parsed the way kubectl parses them — combined short flags are split letter by letter (`-As` is caught), optional-value flags (`--cascade`, `--dry-run`, `--validate`) never swallow the next word, and only `-n`/`--namespace`, `--request-timeout` and `--match-server-version` may precede the verb (or the subcommand of rollout/set/create/auth/cluster-info/top), since kubectl picks the command before parsing flags — so a denied flag or kind cannot hide behind another. Also Denied: RBAC-granting creates (`create role/clusterrole/rolebinding/clusterrolebinding`, `set subject`), `certificate approve`, `set serviceaccount`, `create job --image`, pod-template security patches, plugins and unknown verbs, `--kuberc`, `--as-user-extra`. Denied is the same in every mode: BypassApproval cannot grant more than approval would. SafeWrite means exactly one named object (label/annotate only on a pod or workload kind with a non-reserved key; `scale` to 0, `delete job` and `taint` are RiskyWrite), a write in kube-system / kube-public / kube-node-lease is never auto-approved, and allowlist entries match token by token (`*` is exactly one word). |
| Model | `KubernetesCluster` (+ `RunnerJob.kubernetesClusterId`, `AutoRemediationSuggestion.kubernetesClusterId`) | `aiAccessRunnerId`, `aiAccessCredentialId`, `isAiInvestigationEnabled`, `aiRemediationMode` (Disabled / RequireApproval / Automatic / BypassApproval), `aiKubectlCommandAllowlist`, `aiAccessLastVerifiedAt`, `aiAccessLastError`, `aiAccessConfiguredAt` (server-written, never cleared). Migration `AddKubernetesClusterAiAccess1794400000000`. Loosening a cluster's AI access (Automatic / Bypass approval, the allowlist, a Runner or credential binding) needs `KUBERNETES_AI_ACCESS_ADMIN_PERMISSIONS` (Project Owner, Project Admin or Edit Auto Remediation Rule — what a FullAuto `AutoRemediationRule` needs); binding a credential also needs `KUBERNETES_AI_ACCESS_CREDENTIAL_PERMISSIONS` (credential read). Tightening is open to anyone who may edit the cluster. |
| Readiness | `Common/Server/Services/KubernetesClusterAiAccessService.ts` | Computes `KubernetesClusterAiAccessStatus` from current configuration: every reason AI cannot (fully) use the cluster is a `gap` with a next step and a `blocks` scope, and readiness is derived only from gaps. Also resolves the clusters a signal is about (the subject's `kubernetesClusters` relation, falling back to the Kubernetes monitor step's `clusterIdentifier`). |
| Registration | `POST /runner-ingest/register-kubernetes-agent` (ingestion-key auth, new `TelemetryIngestSurface.KubernetesAgentRunner`) | The chart's Runner exchanges the collector's ingestion key + `clusterName` for a Runner identity. First bind turns investigation on and remediation to "ask for approval" when the chart granted writes; re-registration rotates the key and never touches the operator's switches; a cluster an operator bound elsewhere is never stolen. |
| Jobs | `RunnerJobService.enqueueAiKubectlCommand`, `RunnerJobOrigin.AiInvestigation`, `RunbookStepType.Kubectl` | The chokepoint re-runs the policy, refuses non-Read for investigations, and stores an argv payload. Both AI origins are gated on `Runner.canRunAiCommands`. A credential-less kubectl job is only served to a Runner that reported itself in-cluster. |
| Investigation | `Common/Server/Utils/AI/ClusterAccess/*` | `KubectlInvestigationToolkit` (`list_cluster_access`, `run_kubectl`, Read tier only, 8 per run), `ClusterAccessContext` (deterministic prompt section + persona addendum + the human sentence), `KubectlJobRunner` (enqueue, wait with heartbeat, record outcome, redact/cap). Wired into both investigation runners; `InvestigationRequest.additionalInstructions` appends to the persona. |
| Remediation | `RemediationCommandTools` (+ `CommandPlanExecutor`, `RemediationExecutionRunner`, `AutoRemediationRuleEngineService`) | Cluster targets in the toolkit (`stepType: Kubectl`, `kubernetesClusterId`); FullAuto runs Read/SafeWrite inline and RiskyWrite only when the cluster allowlist names it or the cluster is in BypassApproval; Denied never runs; rollbacks must be safe. The rule engine starts a cluster-level suggestion for every remediation-ready cluster with no rule involved: RequireApproval → Suggest; Automatic → FullAuto + auto-resolve on round 1; BypassApproval → FullAuto + auto-resolve on every round. The verifier's failure path starts one follow-up round — Suggest (for approval), except on BypassApproval clusters where it runs unattended again — capped at 2 rounds per subject per cluster (`MAX_CLUSTER_REMEDIATION_ROUNDS_PER_SUBJECT`). The hourly circuit breaker (`MAX_AUTO_EXECUTIONS_PER_RULE_PER_HOUR`, 3 unattended fixes per cluster) downgrades any unattended run, Bypass included, to a Suggest proposal and posts a note on the incident. |
| Runner | `packages/Runner/Services/KubectlExecutor.ts`, kubernetes-agent mode in `Config.ts` / `RegisterRunner.ts` / `Heartbeat.ts` | Re-runs the policy on the argv, refuses writes when `ONEUPTIME_KUBECTL_ALLOW_WRITES=false`, outside `ONEUPTIME_KUBECTL_WRITE_NAMESPACES` and in its own namespace (`ONEUPTIME_RUNNER_POD_NAMESPACE`), writes a private temporary kubeconfig for credential-based access, spawns pinned kubectl (v1.36.4, in the image) with a minimal environment and kuberc turned off. Heartbeats carry the Kubernetes posture. |
| Chart | `HelmChart/Public/kubernetes-agent/templates/ai-runner.yaml` | Own ServiceAccount; a read-only ClusterRole bound cluster-wide; with `aiAccess.remediation.enabled` a separate `<fullname>-ai-runner-remediation` ClusterRole (namespaced writes) bound by one RoleBinding per `aiAccess.remediation.namespaces` entry, or by a ClusterRoleBinding when the list is empty, and a `<fullname>-ai-runner-node-operations` ClusterRole (nodes patch, pods/eviction create) behind `aiAccess.remediation.nodeOperations` (default true). The release's own namespace cannot be listed. The Deployment passes `ONEUPTIME_KUBECTL_ALLOW_WRITES`, `ONEUPTIME_KUBECTL_WRITE_NAMESPACES` (comma list, empty = cluster-wide) and `ONEUPTIME_RUNNER_POD_NAMESPACE` (downward API) — names shared with the Runner through `Common/Types/Kubernetes/KubernetesClusterAiAccess.ts` — and refuses an `aiAccess.extraEnv` that names any variable the chart sets. Pod Security Admission "restricted"; the image tracks `release` with `imagePullPolicy: Always` (IfNotPresent only for a pinned tag). `tests/ai-runner_test.yaml` asserts each role's rules and every binding by rendering. |
| Dashboard | `Pages/Kubernetes/View/AI.tsx`, `Components/AI/ClusterAccessNotice.tsx`, `RemediationSuggestionCard`, `InvestigationPanel` | The cluster AI page (status, checklist, one-command setup, settings, access test, command history). The investigation panel says "investigated with OneUptime data only — no kubectl access to X, why, and where to fix it" and counts kubectl commands. Kubectl plans render with cluster and tier. |

## Trust model, in one paragraph

Three independent checks keep an investigation read-only: the toolkit refuses non-Read tiers, the enqueue chokepoint refuses non-Read for the investigation origin, and the Runner refuses non-Read for that origin before spawning. Remediation is bounded by three layers that do different jobs. RBAC bounds **where** the in-cluster Runner may write: patch/update on Deployments, StatefulSets, DaemonSets, ReplicaSets and their scale subresource, Jobs, CronJobs, Pods and HPAs, create on Jobs and HPAs, and delete on Pods and Jobs only, in the namespaces `aiAccess.remediation.namespaces` lists (cluster-wide when it is empty), plus node operations cluster-wide; never Secrets, exec/attach/port-forward, ServiceAccount tokens, impersonation, CRDs or RBAC writes. RBAC does **not** bound what a write may do: patch/update on workloads, pods and CronJobs (and create on Jobs) in a namespace is equivalent to running any image as any ServiceAccount there and reading its Secrets, and `apply`/`edit`/`replace`, `--all-namespaces` writes and `delete --all` are ordinary patch/update/delete calls to it. The policy bounds that (tiers, the deny list — pod-template security patches, `set serviceaccount`, `create job --image`, RBAC-granting creates included — the token-wise allowlist, and never an unattended write in kube-system / kube-public / kube-node-lease), and the Runner re-checks it, refuses writes when `ONEUPTIME_KUBECTL_ALLOW_WRITES=false`, outside `ONEUPTIME_KUBECTL_WRITE_NAMESPACES`, and in its own namespace. RBAC cannot exclude namespaces from a ClusterRoleBinding, so on the cluster-wide default the protected and own-namespace lines are held by the policy and the Runner alone; the docs say so. The model never sees a credential: an in-cluster Runner uses its ServiceAccount (and is never handed a credential or used as a Bash/SSH host), an external Runner gets the credential resolved at claim time and written to a private temp file for the life of one command.

## Next epics

- **Hosts / Docker / Podman / other resources.** The same shape: a per-resource AI page, a readiness status with gaps, a Runner binding, and a resource-specific executor with a tiered policy (`systemctl`/`journalctl`/`docker`/`podman` reads; restarts as SafeWrite). The infrastructure agents are push-only today; the Runner (or a Runner mode inside the agent image) is the control channel.
- **Plan preview in the investigation panel.** Today the remediation card sits below the investigation. A single timeline (investigating → commands → root cause → fix → approval → verification) is the next UI step.
- **Per-namespace scoping beyond the chart.** The chart already binds write RBAC per namespace (`aiAccess.remediation.namespaces`) and tells the Runner the list. Next: show that scope on the cluster's AI page from the Runner's posture, have the remediation toolkit refuse an out-of-scope namespace before a plan is composed, and let teams that own part of a cluster scope investigations (read access) the same way.
