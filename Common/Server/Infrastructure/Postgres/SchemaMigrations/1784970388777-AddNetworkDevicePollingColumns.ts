import { MigrationInterface, QueryRunner } from "typeorm";

/*
 * Device-owned polling: the NetworkDevice now carries its own polling
 * schedule and collection options (interface walks, endpoint discovery,
 * health OIDs), previously configured on Network Device monitor steps.
 * The device's assigned probe claims due devices via nextPollAt and the
 * previous walk is kept in lastWalkLog for interface-rate deltas.
 *
 * Generated by generate-postgres-migration and trimmed to the
 * NetworkDevice columns only (the generator also emitted unrelated local
 * schema drift on NetworkDeviceDiscoveryScan/OnCallDutyPolicyScheduleLayer
 * that must not ship in this migration).
 */
export class AddNetworkDevicePollingColumns1784970388777
  implements MigrationInterface
{
  public name = "AddNetworkDevicePollingColumns1784970388777";

  public async up(queryRunner: QueryRunner): Promise<void> {
    await queryRunner.query(
      `ALTER TABLE "NetworkDevice" ADD "isPollingEnabled" boolean NOT NULL DEFAULT true`,
    );
    await queryRunner.query(
      `ALTER TABLE "NetworkDevice" ADD "pollingIntervalInMinutes" integer DEFAULT '5'`,
    );
    await queryRunner.query(
      `ALTER TABLE "NetworkDevice" ADD "walkInterfaces" boolean NOT NULL DEFAULT true`,
    );
    await queryRunner.query(
      `ALTER TABLE "NetworkDevice" ADD "collectEndpoints" boolean NOT NULL DEFAULT false`,
    );
    await queryRunner.query(`ALTER TABLE "NetworkDevice" ADD "snmpOids" jsonb`);
    await queryRunner.query(
      `ALTER TABLE "NetworkDevice" ADD "nextPollAt" TIMESTAMP WITH TIME ZONE`,
    );
    await queryRunner.query(
      `ALTER TABLE "NetworkDevice" ADD "lastWalkLog" jsonb`,
    );
  }

  public async down(queryRunner: QueryRunner): Promise<void> {
    await queryRunner.query(
      `ALTER TABLE "NetworkDevice" DROP COLUMN "lastWalkLog"`,
    );
    await queryRunner.query(
      `ALTER TABLE "NetworkDevice" DROP COLUMN "nextPollAt"`,
    );
    await queryRunner.query(
      `ALTER TABLE "NetworkDevice" DROP COLUMN "snmpOids"`,
    );
    await queryRunner.query(
      `ALTER TABLE "NetworkDevice" DROP COLUMN "collectEndpoints"`,
    );
    await queryRunner.query(
      `ALTER TABLE "NetworkDevice" DROP COLUMN "walkInterfaces"`,
    );
    await queryRunner.query(
      `ALTER TABLE "NetworkDevice" DROP COLUMN "pollingIntervalInMinutes"`,
    );
    await queryRunner.query(
      `ALTER TABLE "NetworkDevice" DROP COLUMN "isPollingEnabled"`,
    );
  }
}
