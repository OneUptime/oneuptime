apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "oneuptime.fullname" . }}-test-connection"
  labels:
    {{- include "oneuptime.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "oneuptime.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
