# The same pre-0.7.0 release upgraded with
#   --reuse-values --set aiAccess.enabled=true --set aiAccess.remediation.enabled=true
# See legacy-pre-0.7.0-ai-enabled.yaml for why the other keys are null.
aiAccess:
  enabled: true
  remediation:
    enabled: true
  image: null
  resources: null
  extraEnv: null
