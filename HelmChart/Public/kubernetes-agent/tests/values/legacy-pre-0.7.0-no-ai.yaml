# A pre-0.7.0 release upgraded with plain `helm upgrade --reuse-values` (the
# README's Upgrading command) and no aiAccess flag at all: .Values.aiAccess is
# nil. The null deletes this chart's default so the templates see exactly that.
aiAccess: null
