apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "oneuptime-ceph-storage.fullname" . }}-test-connection"
  labels:
    {{- include "oneuptime-ceph-storage.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "oneuptime-ceph-storage.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
