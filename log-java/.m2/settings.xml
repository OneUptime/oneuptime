 <settings  xmlns="http://maven.apache.org/SETTINGS/1.0.0" 
            xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
            xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0
            https://maven.apache.org/xsd/settings-1.0.0.xsd">
     
     <servers>
        <server>
          <id>ossrh</id>
          <username>${env.JIRA_USERNAME}</username>
          <password>${env.JIRA_PASSWORD}</password>
        </server>
    </servers>
    <profiles>
      <profile>
        <id>ossrh</id>
        <activation>
          <activeByDefault>true</activeByDefault>
        </activation>
        <properties>
          <gpg.executable>gpg</gpg.executable>
          <gpg.passphrase>${env.GPG_PASSPHRASE}</gpg.passphrase>
          <!-- <gpg.publicKeyring>/home/gitlab-runner/.gnupg/pubring.gpg</gpg.publicKeyring>
          <gpg.secretKeyring>/home/gitlab-runner/.gnupg/secring.gpg</gpg.secretKeyring>
          <gpg.homedir>${env.HOME}/.gnupg</gpg.homedir> -->
        </properties>
      </profile>
    </profiles>
    <activeProfiles>
    <activeProfile>gpg</activeProfile>
  </activeProfiles>
</settings>