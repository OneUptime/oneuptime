{
  "name": "@oneuptime/runner",
  "version": "11.7.4",
  "description": "OneUptime Runner \u2014 one self-hosted agent that executes runbook steps in your own infrastructure and, when enabled, opens AI code-fix pull requests in your repositories.",
  "repository": {
    "type": "git",
    "url": "https://github.com/OneUptime/oneuptime"
  },
  "main": "Index.ts",
  "scripts": {
    "start": "export NODE_OPTIONS=\"--max-old-space-size=8096${NODE_OPTIONS:+ $NODE_OPTIONS}\" && exec node --no-node-snapshot --require ts-node/register Index.ts",
    "compile": "tsc",
    "clear-modules": "rm -rf node_modules && rm package-lock.json && npm install",
    "dev": "npx nodemon",
    "audit": "npm audit --audit-level=low",
    "dep-check": "npm install -g depcheck && depcheck ./ --skip-missing=true",
    "test": "jest --detectOpenHandles --passWithNoTests"
  },
  "author": "OneUptime <hello@oneuptime.com> (https://oneuptime.com/)",
  "license": "Apache-2.0",
  "dependencies": {
    "axios": "^1.13.1",
    "Common": "file:../Common",
    "isolated-vm": "^7.0.0",
    "ts-node": "^10.9.1",
    "ejs": "^3.1.10"
  },
  "devDependencies": {
    "@types/jest": "^27.5.2",
    "@types/node": "^26.1.1",
    "jest": "^28.1.0",
    "nodemon": "^3.1.14",
    "ts-jest": "^28.0.2",
    "typescript": "^5.9.2"
  }
}