## LOG JAVA
audit_log-java:
    stage: BuildAndTest
    allow_failure: true
    script:
        - sudo apt-get update
        - sudo apt-get install -y curl gcc
        - sudo apt-get install -y build-essential
        - curl -sL https://deb.nodesource.com/setup_12.x | sudo -E bash -
        - sudo apt-get install -y maven
        - sudo apt install -y default-jdk
        - mvn --version
        - cd log-java
        - sudo cp .m2/settings.xml /.m2/settings.xml
        - mvn clean
        - cd ..
    except:
        refs:
            - hotfix-master
            - hotfix-release
