# Sentinel — Execution Tracker

> The **living** companion to [AISentinelVision.md](./AISentinelVision.md). Vision says why; this doc says what is built, what is next, and what gates what.
> **Rule:** any PR that changes behavior under `Common/Server/Utils/AI/` (or the legacy `AIAgent/`) must update the relevant status row and, if it re-sequences work, add a line to the Deviations log. Stale status is worse than no status.
> Status last verified against the codebase: **2026-07-14** (six-check verification of the Phase 1/2 status rows + five-reader substrate audit for the Preventive lane; workflow AI substrate added).

---

## 1. Status snapshot

Legend: ✅ Shipped · 🟡 Partial · ❌ Not started · 🔪 To be removed/absorbed

### Phase 0 — Action belt (chat surface)

| Item | Status | Code entry point |
|---|---|---|
| Mutating action-belt tools (9 shipped): `create/acknowledge/resolve_incident`, `acknowledge/resolve_alert`, `page_on_call_policy`, `run_runbook`, `post_incident_status_update`, `change_incident_severity` | ✅ | `Common/Server/Utils/AI/Toolbox/Index.ts` (registration + `isMutationTool` gate), `SentinelActionTools.ts`, `IncidentWriteTools.ts`, `AlertWriteTools.ts` |
| Permission-mode gating of mutations (ReadOnly / AskForApproval / AutoRun) + approval/resume flow | ✅ | `Common/Types/AI/AIChatPermissionMode.ts`, `Common/Server/Utils/AI/Chat/ChatAgentRunner.ts` |
| Code-context read tools (4 shipped): `list_code_repositories`, `find_code_for_exception`, `search_code`, `read_code_file`. Closes the *exception → repo → file → line* half of the causal chain **inside chat** — the first time any AI surface can read customer source. Reuses the already-built resolver (`CodeRepositoryService.resolveRepositoryForException` + `StackTraceRepoResolver` + `StackTraceParser`); the new primitive is `GitHubUtil.getFileContent` (Contents API, **no clone** — a clone cannot fit the 45s per-tool timeout). Read-only, GitHub-App-only (`{contents:read}` least-privilege token), payloads redacted via `Serializer.redact`, `storeContentPreviews:false` already set by the chat runners so source never lands in `LlmLog`. Ambiguity between repos is an error, never a guess | ✅ | `Common/Server/Utils/AI/Toolbox/CodeTools.ts`, `Common/Server/Utils/CodeRepository/GitHub/GitHub.ts` (`getFileContent`), tests in `Common/Tests/Server/Utils/AI/CodeTools.test.ts` |
| Code-context gaps (honesty row): GitLab repos are invisible to the tools (no read path — `repositoryHostedAt` filter excludes them); `search_code` matches **file paths only**, not file contents (GitHub's code-search API does not accept App installation tokens); no tokenizer still exists, so `MAX_LINES_PER_READ = 400` is a conservative guess, not a computed context fit; chat cannot write code or open a PR (that remains the `AIAgent` track) | 🟡 | `Common/Server/Utils/AI/Toolbox/CodeTools.ts` |
| `change_monitor_status` tool | ❌ | — |
| `draft_postmortem` chat tool (auto-postmortem-on-resolve shipped separately, see Phase 3 pull-forwards) | ❌ | — |
| Generic `set_incident_state` (ack/resolve shipped; arbitrary state transitions not) | ❌ | — |

### Phase 1 — Investigation Engine

| Item | Status | Code entry point |
|---|---|---|
| Shared investigation engine: `AIRun(Investigation)` lifecycle, ordered `AIRunEvent` glass-box trail, budgets (8 LLM calls / 12 tool calls / 150s / 2000 output tokens), heartbeats, branded cited output | ✅ | `Common/Server/Utils/AI/Sentinel/SentinelInvestigationEngine.ts` |
| Wake-on-incident trigger — since 2026-07-10 records durable intent (Queued AIRun) via `SentinelInvestigationQueue` before any expensive work; pod restarts can no longer orphan an investigation (D2 closed) | ✅ | `Common/Server/Services/IncidentService.ts` (`onCreateSuccess`) → `Sentinel/IncidentInvestigationRunner.ts` → `Sentinel/InvestigationQueue.ts` |
| Wake-on-alert trigger — same durable-queue path, after the severity/dedupe gates | ✅ | `Common/Server/Services/AlertService.ts` (`onCreateSuccess`) → `Sentinel/AlertInvestigationRunner.ts` → `Sentinel/InvestigationQueue.ts` |
| Read-only enforcement for autonomous runs (hardcoded ReadOnly mode; curated toolbox only, MCP tools never wired in) | ✅ | `Common/Server/Utils/AI/Chat/ObservabilityAssistant.ts` |
| Per-project opt-in: `enableAi` + `enableAutomaticIncidentInvestigation` + `enableAutomaticAlertInvestigation` (two flags, both default **false**) + LLM-provider check | ✅ | `Common/Models/DatabaseModels/Project.ts`, `SentinelInvestigationEngine.isEnabledForProject()` |
| AI settings pages (incidents, alerts) | ✅ | `App/FeatureSet/Dashboard/src/Pages/Incidents/Settings/IncidentAISettings.tsx`, `.../Alerts/Settings/AlertAISettings.tsx` |
| Investigation read API (incident + alert) + live "watch it think" panel (2.5s polling), shared across both subjects; failed runs show `errorMessage` detail | ✅ | `Common/Server/API/AIInvestigationAPI.ts`, `App/FeatureSet/Dashboard/src/Components/Sentinel/InvestigationPanel.tsx` |
| Quiet mode v2 — structured, server-verified confidence signal (deterministic evidence floor over server-minted citations + one constrained classification call, per-consumer fail directions) gates the workspace ping AND the instrumentation trigger; the `INCONCLUSIVE_RE` prose regex is gone (G6) | ✅ | `Common/Server/Utils/AI/Sentinel/ConfidenceSignal.ts`, computed in `SentinelInvestigationEngine.executeRun`, consumed in both runners' `postAnalysis` |
| `baseline_anomaly` read tool — judges a metric's 15-min average against its hour-of-week baseline band (mean ± σ·stddev via `sigmaForSensitivity`); explicit "insufficient baseline data" on cold start; band-chart widget gives `getBandSeries`/`getCoverage` their first callers; investigation persona references it | ✅ | `Common/Server/Utils/AI/Toolbox/MetricTools.ts` (`BaselineAnomalyTool`), tests in `BaselineAnomalyTool.test.ts` |
| Alert-side investigation API endpoint + dashboard panel | ✅ | `POST /ai-investigation/alert` in `AIInvestigationAPI.ts`; shared `Sentinel/InvestigationPanel.tsx` mounted on the alert view *(shipped as a single POST mirroring the incident route — the checklist's "GET/POST" wording was inaccurate)* |
| Alert severity/rate gating — severity floor (explicit per-project minimum via `Project.alertInvestigationMinimumSeverityId`, default = top two tiers) + 30-min per-monitor dedupe window (`AIRun.monitorId`) | ✅ | `AlertInvestigationRunner.shouldInvestigateAlert`, settings UI in `AlertAISettings.tsx`, tests in `SentinelAlertGating.test.ts` |
| RCA also written as incident internal note (bot-authored, created with `ignoreHooks` + `isOwnerNotified` so the quiet-mode-gated RootCause feed item stays the single notification source — no duplicate announcement, no owner page) | ✅ | `Sentinel/IncidentInvestigationRunner.ts` (postAnalysis) |
| **Cost guardrails — Phase 1 exit blockers, see §3** — storm dedupe ✅ (30-min per-monitor window), per-project concurrency cap ✅ (3 concurrent), daily autonomous token budget ✅ (`Project.aiDailyAutonomousTokenLimit`, quiet-skip at run start + hard backstop in `executeWithLogging`, `LlmLogStatus.BudgetExceeded`); kill switch = the default-false opt-in flags (stops new runs; does not abort in-flight) | ✅ | `AlertInvestigationRunner.ts`, `SentinelInvestigationEngine.ts`, `AIService.getAutonomousDailyBudgetStatus`, `LlmLogService.getTotalTokensUsedSince` |

### Pulled forward from later phases (first cuts shipped early)

| Item | Original phase | Status | Code entry point |
|---|---|---|---|
| Auto-postmortem on incident resolve (never overwrites an existing note) | 3 | ✅ | `Common/Server/Utils/AI/Sentinel/IncidentPostmortemRunner.ts`, triggered from `Common/Server/Services/IncidentStateTimelineService.ts` (~line 564) |
| Episodic recurrence memory v1 — retrieval-only, monitor/label overlap over resolved incidents' `rootCause`; no persisted case store, incident-only | 4 | 🟡 | `Common/Server/Utils/AI/Sentinel/SentinelMemory.ts` |

### Platform substrate (relevant existing assets)

| Item | Status | Code entry point |
|---|---|---|
| LLM gateway: multi-provider (incl. keyless local Ollama), metered via `LlmLog` (feature, tokens, cost, billing) | ✅ | `Common/Server/Utils/LLM/LLMService.ts`, `Common/Server/Services/AIService.ts` |
| Tool-free workflow AI: bounded one-shot **Generate Text with AI** component using the configured project/global provider; explicit prompt/system/context egress, provider additional parameters restricted to generation-only tuning (messages/tools/web search/data sources/modalities/storage flags/bounds protected; unknown capabilities fail closed), sensitive values redacted from the AI component's own workflow-log entries + LLM content previews and raw provider errors suppressed from logs/traces, Success/Error routing, token/provider/model/log-id outputs, project/plan access, billing + daily autonomous-budget enforcement, per-project concurrency cap (3), 50k-character input / 4,096-token output / 60s request caps | ✅ | `Common/Types/Workflow/Components/AI.ts`, `Common/Server/Types/Workflow/Components/AI/GenerateText.ts`, `App/FeatureSet/Workflow/Services/RunWorkflow.ts` |
| Prompt caching (Anthropic `cache_control`; OpenAI/Azure `cached_tokens` accounting) — **contrary to the original roadmap, this is done**; `cachedInputTokens`/`cacheCreationTokens` persisted to `LlmLog` since 2026-07-10 and shown in the AI Logs table | ✅ | `Common/Server/Utils/LLM/LLMService.ts` (~654 and ~247), `AIService.executeWithLogging`, `LlmLogsTable.tsx` |
| Token streaming (SSE) | ❌ | `LLMService.ts` hardcodes `stream: false` (~818) |
| Planner/synthesizer model routing | ❌ | one provider/model per call via `LlmProviderService` |
| Stale-run sweeper: since 2026-07-10 REQUEUES heartbeat-stale investigation runs while retry attempts remain (marks Stale only when out of attempts); chat runs marked Stale as before | ✅ | `App/FeatureSet/Workers/Jobs/AIChat/TimeoutStuckRuns.ts` → `SentinelInvestigationQueue.requeueOrMarkStale` |
| Durable claimable AIRun queue + checkpoint/resume — shipped 2026-07-10 (DB-claim on AIRun rows: `Queued` status, `attemptCount`, CAS claims, inline kick + every-minute poller `ProcessQueuedInvestigations`, 30-min TTL, G9 retry policy: transient requeues / permanent finalizes) | ✅ | `Common/Server/Utils/AI/Sentinel/InvestigationQueue.ts`, tests in `SentinelInvestigationQueue.test.ts` |
| Topology graph (product feature): `TelemetryEntityRelationship` model, `DependsOn` edges from span parent/child every 10 min, co-occurrence edges, read-only UI | ✅ | `Common/Models/DatabaseModels/TelemetryEntityRelationship.ts`, `App/FeatureSet/Workers/Jobs/TelemetryEntity/ComputeServiceDependencies.ts` |
| Topology graph consumed by Sentinel (blast radius, causation ordering) | ❌ | nothing under `Common/Server/Utils/AI/` references it |
| Metric baselines: `MetricBaselineHourly` + `getBaseline`/`sigmaForSensitivity` consumed by anomaly monitors; `getBandSeries`/`getCoverage` currently caller-less | ✅ | `Common/Server/Services/MetricBaselineService.ts`, `Common/Server/Utils/Monitor/Criteria/MetricMonitorCriteria.ts` |
| Egress redaction of tool results (JWTs, bearer/AWS/git tokens, credential patterns) | ✅ | `Common/Server/Utils/AI/Toolbox/Serializer.ts` |
| Per-run egress manifest | 🟡 chat only | `ChatAgentRunner.ts`; autonomous Sentinel runs record `AIRunEvent`s but no manifest |
| MTTR raw metrics (TimeToAcknowledge/TimeToResolve per incident) — no `aiInvestigated` dimension, no time-to-RCA metric | 🟡 | `Common/Server/Services/IncidentService.ts` (~2971), `Common/Types/Incident/IncidentMetricType.ts` |
| Deterministic detection surface: hour-of-week metric baseline bands (ClickHouse MV, per-entity cells, 90-day TTL) on 9 monitor types + the `baseline_anomaly` tool. Since 2026-07-14 five deterministic Sentinel Insight detectors extend the sensor surface (exception novelty + spike, error-log spike, trace p99 regression, metric week-over-week drift — insight sources, not monitor types). Remaining gaps: the monitor evaluator still compares entity-scoped series against a **cross-entity** band (W-1 attempted and reverted 2026-07-14 — the monitor pipeline carries no entity identity, and the MV's entity dimension does not correspond to the monitor's attribute dimension; see the W-1 row); `MedianMad` enum never wired; drift is not a monitor criteria filter; no log-novelty detector (Q8); no SLO/error-budget model (Q9) | 🟡 | `Common/Models/AnalyticsModels/MetricBaselineHourly.ts`, `Common/Server/Utils/Monitor/Criteria/MetricMonitorCriteria.ts` (`evaluateAnomaly`), `Common/Server/Utils/AI/Sentinel/Insights/Detectors/` |
| Preventive guardrail schema on `ServiceCodeRepository` (`enableAutomaticImprovements` default **true**, `maxOpenPullRequests` 3, `restrictedImprovementActions`) + `Service`↔repo mapping with `servicePathInRepository` | 🟡 schema only — zero server-side consumers (verified 2026-07-13); the default-true column is a footgun the Preventive lane must flip before shipping an engine | `Common/Models/DatabaseModels/ServiceCodeRepository.ts` |
| Release awareness: `service.version` → `ExceptionInstance.release` (indexed), `TelemetryException.firstSeenInRelease`/`lastSeenInRelease`, `Service.serviceVersion` last-seen overwrite — but version changes emit NO event (hook-bypassing write), keep no history; no Deployment/Release model, GitHub webhook handles only `installation` | 🟡 | `Common/Server/Services/ServiceService.ts` (`updateLastSeen`), `Common/Server/API/GitHubAPI.ts` |

### Legacy AIAgent (FixException) — 🔪 to be absorbed

| Item | Status | Code entry point |
|---|---|---|
| Standalone worker still fully deployed (compose, Helm, KEDA); since 2026-07-13 (B4 Tier 0) it defaults to the in-house metered tool-loop agent — server-mediated completions, no provider key on the worker — and since 2026-07-14 that is the ONLY agent (the OpenCode CLI shell-out, its Dockerfile install, and the `get-llm-config` raw-key endpoint are deleted); registration retries FOREVER with capped exponential backoff (30s→5min) — a slow-booting or briefly-unreachable server can no longer give up an agent container (the 10×30s give-up and the misconfiguration `process.exit` are gone) | 🔪 running in prod | `AIAgent/`, `AIAgent/CodeAgents/InHouseCodeAgent.ts`, `AIAgent/Services/Register.ts`, `docker-compose.base.yml`, `HelmChart/Public/oneuptime/templates/ai-agent.yaml` |
| ~~`get-pending-task` double-processing race~~ — the legacy claim path is GONE with the tables (2026-07-13); the AIRun-substrate claim (`claimNextQueuedCodeFixRun`, CAS on status+attemptCount) is the only claim path. Route names and wire shapes unchanged (`AIAgentTaskAPI` is now a plain protocol router, no CRUD model behind it) | ✅ absorbed | `AIRunService.claimNextQueuedCodeFixRun`, `Common/Server/API/AIAgentTaskAPI.ts` |
| **Legacy tables REMOVED (2026-07-13):** `AIAgentTask`, `AIAgentTaskLog`, `AIAgentTaskTelemetryException` — models, services, CRUD/API registrations, permissions (`*AIAgentTaskTelemetryException` enum values dropped; `*ProjectAIAgentTask` kept — they govern the surviving `AIAgentTaskPullRequest`), the two legacy sweeper jobs (`AIAgent:TimeoutStuckTasks`, `AIAgent:FailOrphanedScheduledTasks`), and `AIAgentTaskPullRequest.aiAgentTaskId`. Historical task rows die with the tables (their PRs survive on `AIAgentTaskPullRequest`); drop migration hand-written | ✅ removed | migration `DropLegacyAIAgentTaskTables…`; `App/FeatureSet/Workers/Jobs/AIAgent/FailOrphanedQueuedCodeFixRuns.ts` (the surviving CodeFix orphan sweeper, renamed from the old file) |
| Git/PR/workspace plumbing to keep (fold into future `code_fix` tool; GitHub-only today, generalize via `CodeRepository.repositoryHostedAt`) | ✅ exists | `AIAgent/Utils/PullRequestCreator.ts` and related |
| Fix-context ceiling + no verify loop (honesty row, updated 2026-07-13): handler reads ONLY exception message/stackTrace/type/fingerprint + service name — never logs, traces, spans, or occurrence history; only gate before the PR is `filesModified.length > 0` (no build/test/lint) — but the PR now opens as a **draft** (non-draft fallback when the repo rejects drafts); CodeFix runs are never auto-retried (the capless stuck-task reset died with the legacy tables — heartbeat-stale runs finalize as Error) | 🔪 context ceiling + CI verify remain for the code-fix track | `Common/Server/API/AIAgentDataAPI.ts`, `AIAgent/TaskHandlers/FixExceptionTaskHandler.ts` |

---

## 2. Safety gates

The vision doc's §6 commitments, tracked honestly. **None of the first four are met today** — mutations shipped ahead of them under interim mitigations. Each gate names what it blocks: that work does not ship until the gate is green.

| # | Gate (vision §6 commitment) | Status | Interim mitigation in force | Blocks |
|---|---|---|---|---|
| G1 | Policy gateway (risk tiers, blast-radius limits, remediation-storm circuit breaker, capability-scoped tokens) | ❌ NOT MET | Tool-level `requiredPermissions` RBAC; per-conversation permission modes; per-turn budgets; 45s tool timeout; autonomous runs hardcoded ReadOnly | Any autonomous mutation; any default-on AutoRun; Phase 3 remediation features |
| G2 | Undo / counterfactual dry-run per mutation | ❌ NOT MET | AskForApproval mode shows the tool call before execution | Phase 2 exit; marketing any "reversible by design" claim |
| G3 | Accuracy-scored autonomy graduation (eval harness + thresholds) | ❌ NOT MET | AutoRun is a manual, per-conversation, human choice — never a default. **Since 2026-07-13 the measurement half exists:** `npm run ai-eval` (`Common/Server/Utils/AI/Eval/`) scores labeled recorded runs on the four roadmap axes (top-hypothesis precision via autoGrade, human confirmed rate — reported separately, never blended — citation grounding, tool selection, inconclusive recall) with honest null-when-unmeasurable semantics and a loud small-sample warning below 20 cases. What still blocks the gate: an actual ≥50-labeled-case corpus, agreed graduation thresholds, and full offline replay (needs recorded tool result payloads — events carry summaries only) | Any autonomy expansion; public accuracy scorecard; page-delay pilot |
| G4 | Cost guardrails: per-project concurrent-investigation cap, per-feature budget **enforcement**, storm dedupe, project kill-switch | ✅ MET (2026-07-10) | Storm dedupe (30-min per-monitor window), concurrency cap (3/project), severity floor, and daily autonomous token budget (quiet-skip at run start, fail-closed backstop in `executeWithLogging`) all enforced. Residuals, tracked honestly: budget is one per-project autonomous pool (not per-feature); no default limit ships until Q4 sets plan quotas — so **cloud default-on still waits on Q4**; kill switches = opt-in flags or daily limit 0 (both stop new runs; neither aborts in-flight ones) | ~~Phase 1 closeout~~ satisfied for closeout; default-on anywhere still needs Q4 quota defaults |
| G5 | Egress manifest on all autonomous runs + documented cloud egress/DPA posture + redaction beyond tool results | 🟡 PARTIAL | `Serializer.ts` redaction on tool results; manifest on chat runs; self-host/BYO-LLM = zero third-party egress. **Since 2026-07-13 (B4 Tier 0) the code-fix worker no longer holds LLM provider keys** — agent completions are server-mediated, so the worker container's egress surface is git push + the OneUptime API only (unconditional since 2026-07-14: the `CODE_AGENT_TYPE=OpenCode` raw-key fallback and the `get-llm-config` endpoint are deleted) | SOC2/enterprise packaging; "no training on your data" claim for cloud tenants |
| G6 | No control-flow from free-form prose (structured, server-verified confidence signals) | ✅ MET for every shipped consumer (2026-07-13) | The `INCONCLUSIVE_RE` prose regex is replaced by `ConfidenceSignal.ts`: (1) a deterministic evidence floor over the run's server-minted citations — zero citations AND zero data-bearing tool calls → inconclusive regardless of prose (prose cannot fake evidence never minted); (2) only past the floor, ONE constrained classification call (temperature 0, 20-token cap, word-bounded CONFIDENT/INCONCLUSIVE parse — the grader idiom). Per-consumer fail directions when the classification errs/unparses: the workspace ping SENDS (quiet mode fails louder, not silent) and the instrumentation trigger does NOT enqueue (autonomous PR creation fails toward doing nothing). Residuals, tracked honestly: the classifier is still an LLM judging prose — but its output is a two-token verdict, deterministically floored and per-consumer fail-safed, never free-form control flow; future consumers (HypothesisBoard VALIDATED/INVALIDATED labels, page-delay) do not exist yet and MUST use this pattern when they ship | Page-delay (§4.6) still blocked on G3+G7; HypothesisBoard labels must adopt this signal when built |
| G7 | Page-suppression safety contract (delay-never-cancel, dead-man's switch, per-service opt-in, suppression-precision reporting, mandatory review queue) | ❌ NOT MET (feature not built) | Feature does not exist; do not build without this contract | All of §4.6 |
| G8 | Audit-trail lifecycle: configurable `LlmLog` retention + restrict prompt/response preview reads to admin-tier; same review for `AIRunEvent` payloads | 🟡 PARTIAL | Since 2026-07-10 NO path stores prompt/response previews: chat/investigations already opted out, and the five one-shot endpoints + auto-postmortem now do too (their prompts embed content with narrower ACLs than LlmLog). Retention remains hard-coded 3-day on cloud / indefinite self-host — the configurable, access-tiered lifecycle is still open | Enterprise/SOC2 packaging |
| G9 | Failure semantics per capability (pages fail open; mutations/comms fail closed; verification fails to "unverified"; failures visibly surfaced) | 🟡 PARTIAL | Investigation failures mark the AIRun `Error` and log; since 2026-07-10 the investigation panel (incident + alert) shows the run's `errorMessage` detail on failure; still nothing posted to the feed or workspace. **Since 2026-07-13 (B4 Tier 1) the verification-fails-to-unverified clause is enforced in shipped code:** fix-PR CI conclusions count as verified ONLY on `Green`/`ExpectedFailureObserved` — a repo without CI reads `NoCiConfigured`, an unpolled row stays null, unknown check-run conclusions count as failed, and an App installation without checks-read degrades to unpolled; all of those read as unverified everywhere (the verified-green rate counts them AGAINST the rate, never toward it) | Phase 3 verify/rollback; §4.9 comms |
| G10 | Memory safety (tenant isolation at storage layer, provenance + review on writes, quarantine/expiry, retrieved-memory-as-untrusted) | 🟡 N/A today (memory v1 is read-only, project-scoped, stores nothing) | `SentinelMemory.ts` scopes by `projectId`, no writes | Phase 4 persisted cases, pgvector RAG, correction memory, runbook registry |
| G11 | Preventive precision + noise budget (vision §4.14/§6): per-finding-type measured precision (human confirm/dismiss rates) gates any automation; per-repo `maxOpenPullRequests` + per-project daily fix-task budget + fingerprint dedupe enforced; fix PRs human-reviewed always — auto-merge is a non-goal, not a setting | 🟡 PARTIAL (guardrails enforced 2026-07-13; measured precision + findings lane still missing) | **Since 2026-07-13 the fix-pipeline guardrails are ENFORCED, not just specced:** per-project daily fix-run budget (`Project.aiDailyFixTaskLimit`, default 25/day, 0 pauses — `FixRunBudget`, checked at BOTH creation paths so every recipe and trigger is covered), per-repo open-PR cap (`CodeRepository.maxOpenFixPullRequests`, default 5, enforced server-side at the repository-token gate — a repo at its cap cannot receive a push), and **fix PRs open as drafts** (non-draft fallback when the repo rejects drafts). Exception-fix path remains manual-only: user click per exception, one active task per (exception, recipe); FixFromIncident/FixPerformance likewise user-click-only, one active run per subject/trace. The ImproveInstrumentation carve-out (auto-created from an inconclusive investigation) stays behind its default-FALSE opt-in + per-subject dedupe, and now also inside the daily budget (over-budget = logged skip, never an investigation failure). Task creation fail-early validates prerequisites (readiness API); since 2026-07-13 (B4 Tier 0) agent LLM calls are server-mediated and metered — the worker holds no provider key at all, a project-owned provider is preferred, and the global provider serves as the metered fallback on cloud too (unconditional since 2026-07-14: the `CODE_AGENT_TYPE=OpenCode` raw-key fallback is deleted, and the ai-fix readiness check now passes on cloud with no project-owned provider — while also verifying the project's AI balance when the costed global provider would serve the run, so readiness stays fail-early). **Since 2026-07-13 (B4 Tier 1) the verified-green rate exists:** open fix PRs' check-run conclusions are read from the customer's OWN CI (never re-run, never gated) into `AIAgentTaskPullRequest.ciStatus`, and the outcomes card reports merged-and-CI-green beside the acceptance rate — the precision baseline now has a quality axis, not just a merge axis (should-fail regression-test PRs report RED as `ExpectedFailureObserved`; no CI = `NoCiConfigured` = never verified). **Since 2026-07-14 the findings inbox EXISTS and confirm/dismiss instrumentation lives on the findings themselves:** `SentinelInsight` + the Sentinel → Insights inbox, with `humanVerdict`/`humanVerdictAt`/`humanVerdictByUserId` captured via the `POST /sentinel-insight/verdict` + `/resolve` endpoints and one-click Confirm/Dismiss/Resolve on the insight page — the per-finding-type precision measurement this gate demands is now collected at the source (a Dismiss also suppresses recreation for 7 days, the noise-budget half). The insight-driven auto-fix paths (FixException for new/spiking exceptions, findings-based FixPerformance for latency regressions) shipped the same day behind the default-FALSE `Project.enableInsightFixTasks` opt-in — the ImproveInstrumentation posture extended, inside every enforced guardrail (see D8). **What keeps the gate from green:** precision thresholds/graduation logic (verdicts now accumulate, but nothing gates on them yet — today's automation gate is an explicit product-owner opt-in, not an earned precision threshold), pre-PR verification (Tier 1 verifies on the draft PR AFTER it opens; Tier 2's sandbox is what verifies before), and merged-with-edits detection | Any default-on or precision-graduated preventive automation (the shipped insight-driven fix tasks stay explicit-opt-in, default false — D8); any "incidents prevented" claim |

---

## 3. Phases (re-baselined 2026-07-10)

Ordering changed from the original roadmap — rationale in the Deviations log. Each item is a checkbox with a binary definition of done; a phase exits only when its checklist and its named gates are green.

### Phase 1 closeout — finish and harden what shipped ← **CURRENT**

- [x] `baseline_anomaly` read-only tool registered in the Toolbox and exercised in a Sentinel run (wraps `MetricBaselineService.getBaseline`/`sigmaForSensitivity`; also gives `getBandSeries`/`getCoverage` their first callers) *(shipped 2026-07-10; "exercised in a Sentinel run" pending the production dogfood — the persona now instructs the model to use it)*
- [x] `POST /ai-investigation/alert` + alert-side investigation panel merged *(shipped 2026-07-10; panel generalized to `Sentinel/InvestigationPanel.tsx`, failed runs now surface `errorMessage` detail on both subjects — the panel half of the Phase 2 G9 item)*
- [x] Alert severity/rate gating: investigations only for alerts meeting a per-project severity floor, with a per-monitor/episode dedupe window *(shipped 2026-07-10: default floor = top two tiers, per-project override, 30-min per-monitor window — Q2 answered)*
- [x] **G4 cost guardrails (exit blocker):** at most one active investigation per monitor/episode per window; per-project concurrent-investigation cap; per-feature daily token/cost budget enforced in `AIService.executeWithLogging`; project-level autonomous-runs kill switch *(shipped 2026-07-10 — budget is one per-project pool over the autonomous features rather than per-feature (split when more autonomous features exist); default = no limit until Q4 sets plan quotas; kill switch = the default-false opt-in flags, which stop new runs but do not abort in-flight ones)*
- [x] RCA posted as incident internal note in addition to the feed item (or the Appendix claim formally dropped — decide, don't drift) *(decided + shipped 2026-07-10: note is bot-authored with no duplicate announcement; alert-side note parity is an optional follow-on, not claimed)*
- [x] Persist `cachedInputTokens` to `LlmLog` for cache-hit visibility *(shipped 2026-07-10: `cachedInputTokens` + `cacheCreationTokens` columns, written in `AIService.executeWithLogging`, cached count shown in the AI Logs table)*
- [x] **Standalone patch, ships independently:** atomic-claim CAS fix for `get-pending-task` in `Common/Server/API/AIAgentTaskAPI.ts` *(shipped 2026-07-10: `AIAgentTaskService.claimNextScheduledTask`)*
- [x] Docs page (`Docs/Content/en/...`): opt-in flags, quiet mode, budgets, BYO-LLM/Ollama setup — the shipped flagship currently has **zero** user docs *(shipped 2026-07-10: `en/ai/sentinel.md` covering enablement, quiet mode, all five cost controls, trust guarantees, and the auto-postmortem; nav entry + cross-link from the LLM Providers page; English-only day one — locale fallback covers the other languages until translated)*
- [ ] Measured exit: median trigger→RCA-posted < 3 min over ≥20 real investigations on our own production project

### Phase 2 — Durability, measurement, trust UX

**Hard rule: the durable queue lands before any new autonomous trigger (predictive sweep, storm collapse, anything).**

- [x] Durable claimable AIRun queue: atomic claim, checkpoint/resume, restart-safe; replace the detached triggers in `IncidentService.onCreateSuccess` / `AlertService.onCreateSuccess`; generalize the CAS + `pausedState` pattern from the chat approval-resume path; retry policy for transient failures (G9) *(shipped 2026-07-10 as `SentinelInvestigationQueue` — Q1 decided: DB-claim on AIRun rows (new `Queued` status + `attemptCount`), enqueue-before-work + inline kick + every-minute poller + requeue-on-stale + 30-min queue TTL. Checkpointing is at attempt granularity (re-run from top — safe, runs are read-only); mid-run message-level checkpointing deferred until something needs it. D2 residual closed.)*
- [x] Measurement plumbing (feeds vision §8 metrics and G3): human verdict field on `AIRun` (confirmed/edited/rejected) captured via one-click control on the investigation panel; on-resolve grading job comparing the top hypothesis to `Incident.rootCause`/postmortem; `time-to-rca` incident metric; `aiInvestigated` dimension on incident metrics; internal before/after MTTR report *(plumbing shipped 2026-07-13 — see changelog: `AIRun.humanVerdict/humanVerdictAt/humanVerdictByUserId` via `POST /ai-investigation/verdict` (two-button Confirmed/Rejected — "edited" is the grading side's job, not a human button), on-resolve `InvestigationGrader` storing `AIRun.autoGrade` Match/Partial/Mismatch, `oneuptime.incident.time-to-rca`, `aiInvestigated` on MTTA/MTTR. Residuals tracked here: the internal before/after MTTR REPORT is an analysis artifact that needs accumulated data, not plumbing; grading is incident-only (alerts have a `rootCause` column but no human resolution flow populates it — skipped deliberately); grading vs the postmortem (not just `rootCause`) not attempted)*
- [ ] Eval harness bootstrap (G3): golden-incident corpus from real resolved incidents (≥50), offline replay from recorded `AIRunEvent` trails, scored on top-hypothesis precision / citation-grounding / tool-selection / inconclusive-recall *(scoring + corpus-export half shipped 2026-07-13 — `npm run ai-eval` exports every LABELED completed investigation (humanVerdict or autoGrade set) as a `GoldenCase` JSON corpus and computes all four scores from the recorded trails + labels with NO LLM re-run; every score is `{value, numerator, denominator}` with value null when unmeasurable, and a <20-case corpus is loudly flagged as untrustworthy. Still open, keeping the box unchecked: the ≥50-case corpus itself (labels must accumulate from panel verdicts + on-resolve grades) and FULL offline replay — structurally impossible from today's trails because `AIRunEvent` persists tool arguments + result summaries (rowCount/duration/bytes) but never the result payloads the LLM read; `Common/Server/Utils/AI/Eval/ReplayInvestigation.ts` documents the exact prerequisites (record redacted serialized tool results per ToolCallCompleted under G8 review, recorded-result tool transport, pinned model config))*
- [x] Replace `INCONCLUSIVE_RE` with a structured, server-verified confidence signal (G6) — constrained classification call or deterministic checks over cited evidence *(shipped 2026-07-13: BOTH — deterministic evidence floor over server-minted citations, then one constrained classification call; per-consumer fail directions (ping fails louder, instrumentation-PR trigger fails toward no PR); Q3 answered — see changelog)*
- [ ] Minimal policy gateway v1 (G1): per-tool risk tiers + mutation-rate circuit breaker
- [ ] Undo + dry-run for undoable mutations; irreversible-action labeling for the rest (G2)
- [ ] Topology consumption (pulled from Phase 4 — graph is already built): `get_service_dependencies` read tool + blast-radius/dependency context injected into investigations *(built and REVERTED 2026-07-11 — see Deviations D6: the service map itself is not production ready; re-add from git history (commit 13455808bf) once it is)*
- [ ] Event-level push (SSE or long-poll over `AIRunEvent`s) to kill the 2.5s panel poll — **token-level streaming deferred** until after the queue, so the transport is designed once against the claimed-run model (the original "load-bearing for the demo" claim is obsolete: the shipped panel already narrates)
- [ ] HypothesisBoard + CausalChain widgets with deterministically-earned labels (G6 applies)
- [ ] `LlmLog` retention + preview-access tiering; ~~`storeContentPreviews: false` on postmortem/one-shot endpoints~~ *(preview half shipped 2026-07-10 — all five one-shot endpoints + the auto-postmortem path now set it false; retention/access-tiering still open)* (G8)
- [ ] Egress manifest extended to autonomous runs (G5)
- [ ] Surface failure *detail* for failed/stale investigations — ~~errorMessage/stale reason in the panel~~ *(panel half shipped 2026-07-10 with the alert-panel work)* plus a quiet feed marker (G9) — feed marker still open
- [ ] Remaining Phase 0 tools: `change_monitor_status`, `draft_postmortem`, generic `set_incident_state`
- [ ] Activation: discovery banner on incident view for projects with `enableAi=true` but investigation flags off; changelog + blog post

### Phase 3 — Proactive detection, active verify, legacy absorption

- [ ] Pre-incident detection as **anomaly-monitor-triggered investigation enrichment** — existing anomaly monitors fire, Sentinel investigates proactively; explicitly NOT a second anomaly cron (original BUILD note stands); gated on the queue + circuit breaker *(complementary to the Preventive lane below: this item opens/enriches incidents from loud anomalies; the lane files quiet findings with no incident at all)*
- [ ] Alert-storm collapse ("47 alerts, 1 root cause") — rule-based grouping + AI root-cause grouping, on the queue
- [ ] Active verification: re-run the exact failed probe/synthetic monitor on demand; report "verified recovered" / "unverified" only (G9)
- [ ] Deterministic deploy correlation + one-click rollback of the identified release
- [ ] Confidence-gated page **delay** pilot — only under the G7 contract, only after G3/G6 are green, opt-in per service
- [ ] Auto-postmortem hardening (shipped early): move onto the durable queue; human-review affordance
- [ ] **KILL/ABSORB milestone for legacy AIAgent:** freeze new legacy-handler development now; cut the TelemetryException enqueue over to the unified runtime when `code_fix` ships; remove compose/Helm/KEDA wiring; until absorbed, the legacy agent is explicitly exempt from the §6 trust claims and marketed accordingly *(partial 2026-07-13: the `code_fix` recipe itself now exists — FixFromIncident, human-triggered from the investigation panel — and the enqueue already lives on the AIRun substrate (B1); the legacy `AIAgentTask`/`AIAgentTaskLog`/`AIAgentTaskTelemetryException` tables are now REMOVED (models, services, permissions, sweepers, drop migration) — the data-model absorption is complete; and since 2026-07-13 (B4 Tier 0) execution defaults to the in-house metered sub-agent — the OpenCode fallback was deleted 2026-07-14, so what remains is the compose/Helm/KEDA infra removal)*
- [ ] Weekly "made your software better" digest

### Code-fix track (split out — runs parallel to Phases 3–4; the largest lift in the roadmap)

- [x] **Design spike first, before any code:** per-repo CI sandbox — isolation model, resource limits, secrets handling, air-gapped behavior, multi-tenant cost *(shipped 2026-07-13: [CodeFixSandboxDesign.md](./CodeFixSandboxDesign.md) — two tiers: Tier 0 server-mediated metered agent, Tier 1 customer-CI verification on draft PRs, Tier 2 ephemeral gVisor sandbox deferred until usage justifies it; implementation approved by founder)*
- [x] In-house `LLMService` coding sub-agent replaces the OpenCode CLI shell-out; reuse `RepositoryManager`/`PullRequestCreator`/`WorkspaceManager` plumbing *(shipped 2026-07-13 — B4 Tier 0, see changelog: server-mediated metered completions (`/ai-agent-data/llm-completion`, feature "Sentinel Code Fix", per-run loop budgets 40 calls / 100k output tokens), `InHouseCodeAgent` tool loop with path-guarded workspace tools + per-tool ProgressLog narration, worker holds no provider key, global provider re-enabled on cloud (metered). **Residual resolved 2026-07-14:** the OpenCode shell-out, its Dockerfile install, the `get-llm-config` raw-key endpoint, `BackendAPI.getLLMConfig`, and `getLlmProviderForAgentTasks` are deleted — the in-house metered agent is the only code agent)*
- [ ] Regression test generated from the real production error; build/test/lint verify loop until green *(partial 2026-07-13: the recipe shipped in B2, and since B4 Tier 1 the SHOULD-FAIL outcome is OBSERVED — a RED CI conclusion on a `WriteRegressionTest` draft PR records `ExpectedFailureObserved` via the customer's own checks, with the honest caveat that check-run granularity cannot tell which job failed; the in-sandbox verify-until-green loop itself remains open — Tier 2)*
- [ ] GitLab support (honor `CodeRepository.repositoryHostedAt`)

### Preventive lane — silent watch → finding → fix-before-incident (added 2026-07-13; runs parallel to Phases 2–4)

> Vision §4.14. Architecture rule: **no LLM in the watch loop** — deterministic sensors do the always-on watching; the LLM engages per-finding (budgeted, read-only, on the durable queue); the coding agent engages only for finding types that have earned it (G11). Substrate audit 2026-07-13: the guardrail schema already shipped with **no engine** — `ServiceCodeRepository.enableAutomaticImprovements` (default **true**), `maxOpenPullRequests` (default 3), `restrictedImprovementActions` ({Fix Exceptions, Improve Logs, Improve Spans, Improve Metrics, Fix Performance Issues}) have zero server-side consumers. The manual `Fix with AI Agent` flow is the only fix path today: user-click-only, context ceiling = stack trace + exception message + service name (never logs/traces/occurrences — `AIAgentDataAPI.ts:135`), no CI verify before the non-draft PR, GitHub-only. The detection surface is metric-band-only: no drift/trend, no log novelty, no trace-latency regression, no exception-rate anomaly, no SLO model (Q9). *(That audit is the 2026-07-13 baseline this lane shipped against — the first full slice (five deterministic detectors → `SentinelInsight` inbox → per-finding triage → opt-in fix routing) landed 2026-07-14; the row annotations below and the changelog entry carry the honest per-item state.)*

**W — deterministic watchers (product features, no LLM, can ship independently):**

- [ ] Per-entity baseline fix: `evaluateAnomaly` calls `getBaseline` without `primaryEntityId` (`MetricMonitorCriteria.ts:~850`), so the band aggregates across every entity emitting the metric name even though the MV stores per-entity cells — **NOT shipped; blocked, and deeper than it looks** *(attempted 2026-07-14 with the Insights slice and REVERTED the same day, before push: the shipped `resolveSeriesPrimaryEntityId` was inert — adversarial review traced the real pipeline and found the monitor carries no entity identity to thread. `monitorMetric` queries `{projectId, time, name, attributes?}`; `MetricService.toAggregateStatement` selects only `[value, time, ...groupBy, attributes?]` — `primaryEntityId` is never selected or grouped; series labels come from `extractSeriesLabels`, which reads OTel `attributes` keys only, and `primaryEntityId` is a top-level ClickHouse column, not an attribute. **The deeper blocker:** `MetricBaselineHourly_mv` groups by `(projectId, name, primaryEntityId, day, hourOfWeek)` — baseline cells are scoped to the telemetry SERVICE, while monitor series are scoped to OTel ATTRIBUTES (`host.name`) — the two dimensions do not correspond, so W-1 as written cannot be satisfied even with identity threaded. Unblocking needs its own PR: either group the monitor aggregation by `primaryEntityId` (changes row cardinality → changes the samples EVERY criteria in the step evaluates, incl. static thresholds and evaluate-over-time → changes which alerts fire) or add the grouped-attribute dimension to the baseline MV (schema change). `MetricBaselineService.getBaseline`'s optional `primaryEntityId` param stays — `MetricTools.ts` uses it, where the agent supplies an explicit id.)*
- [ ] Week-over-week drift detector over the existing `MetricBaselineHourly` per-day rows (anchored-window vs rolling comparison; the rolling band re-learns slow decay and structurally cannot see it) — new criteria filter type on existing monitors + a finding source, NOT a second anomaly scheduler *(finding-source half shipped 2026-07-14 as the `MetricDrift` insight detector: new `MetricBaselineService.getWeekOverWeekDrift` reads the existing per-day baseline rows (day ≥ today−7 vs day between today−14 and today−8; both windows ≥100 samples; |relative mean change| ≥50%; top 5 by |change|; always Low severity — drift direction says nothing about goodness — and never auto-fixed). It reads learned baselines; it does not re-evaluate metric bands — the non-goal stands. The monitor-criteria filter-type half stays open, keeping this box unchecked)*
- [ ] Release-change signal: `ServiceService.updateLastSeen` writes `serviceVersion` via `updateColumnsByIdWithoutHooks` (no event, no history) — emit a first-class release-changed event + short version history, then a post-deploy regression watch (post-deploy window vs same-hour baseline; also strengthens the reactive §4.4 deploy correlation)
- [ ] Exception novelty/regression detectors: new-fingerprint signal + regressed-in-newer-release (`TelemetryException.firstSeenInRelease`/`lastSeenInRelease` exist; recurrence today re-opens with no release-aware logic) *(novelty + spike halves shipped 2026-07-14 as SentinelInsight sources: `NewExceptionDetector` (fingerprint first seen ≤24h with ≥3 occurrences; High at ≥50) and `ExceptionSpikeDetector` (established fingerprints at ≥10 occurrences in the last hour AND ≥5× the prior-24h hourly rate — a dormant fingerprint waking up falls out of the same math; High at ≥10×). The release-aware regression half is still open: recurrence remains release-blind, keeping this box unchecked)*
- [x] Trace p95 latency regression per (service, operation) — needs a spans baseline MV modeled on `MetricBaselineHourly` *(shipped 2026-07-14 WITHOUT the new MV, and deliberately as **p99, not p95**: `TraceLatencyRegressionDetector` compares per-service p99 recent-1h vs prior-24h through the existing span projection (Count/Avg/P99 are projection-served; p95 is not — a p95 cut would need the spans baseline MV this row originally called for). Regression = recent p99 ≥1s AND ≥2× prior AND ≥100 prior samples (High at ≥4×); detection is per service, then the drill names the slowest operation (fingerprint `latency:<service>:<op>`) and stores `SpanTreeAnalyzer` findings + code locations from one representative trace as evidence that outlives span retention. Honest residuals: single-trace evidence (no cross-trace frequency), volume-ranked top-25 services scanned per tick)*
- [ ] Log-pattern novelty (template mining / clustering) — biggest net-new detector; design spike first (Q8)

**F — findings substrate:**

- [x] `ReliabilityFinding` model + quiet triage inbox — clone the `TelemetryException` lifecycle shape (fingerprint dedupe, firstSeen/lastSeen, occurrenceCount, assign, resolve/dismiss + audit fields); **findings never page** (non-goal below); feeds the Phase 4 reliability-debt report *(shipped 2026-07-14 as the `SentinelInsight` model + the **Insights inbox** (Sentinel nav → Insights, `/dashboard/:projectId/ai/insights`): fingerprint dedupe against the most recent (projectId, fingerprint) row, firstSeenAt/lastSeenAt/occurrenceCount refresh on recurrence, Detected → ActionRequired|FixOpened → Resolved|Dismissed lifecycle, human Confirm/Dismiss with audit (`humanVerdict`/`humanVerdictAt`/`humanVerdictByUserId`), 7-day recreation cooldown after a human dismissal. Insights never page — nothing touches the notification pipeline, per the non-goal. Deliberately not built: assign (nothing needed it for v1))*
- [x] Preventive Sentinel triage on the durable AIRun queue: read-only, budgeted, cited enrichment per finding (service attribution → repo via `ServiceCodeRepository`, impact estimate, suggested action); human confirm/dismiss instrumentation from day one (this IS the G11 measurement) *(shipped 2026-07-14: every new insight enqueues a read-only Investigation AIRun on the existing durable queue — new `AIRun.triggeredBySentinelInsightId` subject; CAS claims, retries, TTL, and the stale sweeper all inherited unchanged — budgeted under the new AUTONOMOUS feature **"Sentinel Insight Triage"** (the G4 daily token budget covers it at enqueue, claim, and every LLM call), deduped to one non-terminal triage run per insight, quiet-skipped without a provider or budget headroom. The preventive persona asks for root cause, blast radius, and ONE next action, cited; the result is persisted to the insight as `triageSummaryMarkdown`/`triageCompletedAt` — quiet inbox enrichment only, no feed items, no notifications. Confirm/dismiss instrumentation shipped with the model, on the finding itself. Note: `ServiceCodeRepository` died 2026-07-13 — repo attribution rides the fix-routing path's runtime resolution instead)*

**X — fix-before-incident:**

- [ ] Outcome instrumentation on the EXISTING manual fix path first (PR merged / closed-unmerged / merged-with-edits, from `AIAgentTaskPullRequest` state) — zero-risk, starts the G11 precision baseline immediately *(state-sync shipped 2026-07-13: `AIAgent:SyncPullRequestStates` polls open agent PRs every 30 min via the GitHub App and records Merged/Closed; rate reporting shipped same day: `getOutcomeStats` + `GET /ai-agent-task-pull-request/outcome-stats` + "Fix Pull Request Outcomes" card on the AI tasks page — acceptance rate is null until a terminal outcome exists, never 0-or-100 noise; merged-with-edits detection still open)*
- [x] Flip `enableAutomaticImprovements` default to **false** + explicit project-level preventive opt-in flag (the column shipped default-true with no engine; when an engine lands it must not become silent default-on — same opt-in posture as the investigation flags) *(shipped 2026-07-14 as `Project.enableSentinelInsights` (the watch/inbox/triage opt-in) + `Project.enableInsightFixTasks` (the fix-automation opt-in, gated strictly `=== true` in `InsightFixRouting`), both default **false**, toggles on the Insights settings page — the engine landed and did NOT become silent default-on. The `enableAutomaticImprovements` half is historical: that column died with `ServiceCodeRepository` on 2026-07-13, so there was nothing left to flip)*
- [ ] Auto-created fix tasks for graduated finding types only (new-fingerprint exceptions first — highest precision, handler exists), honoring per-repo guardrails *(✅ re-introduced 2026-07-13 as `CodeRepository.maxOpenFixPullRequests` — default 5, enforced server-side at the repository-token gate, no token = no push; `restrictedImprovementActions` deliberately NOT resurrected until an automation exists to restrict)* + a per-project daily fix-task budget *(✅ shipped 2026-07-13: `Project.aiDailyFixTaskLimit`, default 25/UTC-day, 0 pauses — `FixRunBudget` enforced at both creation paths, covering every recipe and trigger)* + a retry cap *(✅ moot 2026-07-13: the capless 30-min stuck-task reset died with the legacy `AIAgentTask` tables — CodeFix AIRuns are never auto-retried; heartbeat-stale runs finalize as Error)*; **draft PRs *(✅ shipped 2026-07-13: all fix PRs open as GitHub drafts via the shared `PullRequestCreator`, with a logged non-draft fallback when the repository rejects drafts)*, human review always**; requires the code-fix track's sandbox + verify loop (today's path still commits the agent diff with zero build/test/lint before the PR opens — **pre-PR verification is now the only guardrail on this list still missing**: since 2026-07-13 (B4 Tier 1) the draft PR's conclusion IS verified post-hoc through the customer's own CI (`ciStatus` + verified-green rate), but nothing runs the code before the PR exists — that is Tier 2) *(first auto-created recipe shipped 2026-07-13 outside the findings lane: **ImproveInstrumentation** — the vision §5 "instrumentation-gap ticket", upgraded to a PR, triggered by an inconclusive investigation; default-false `Project.enableInstrumentationFixTasks` opt-in + per-subject dedupe; since the guardrail shipment it also honors the daily budget (over-budget = logged skip) and opens drafts)* *(insight-driven engine shipped 2026-07-14 BEHIND the default-false `Project.enableInsightFixTasks` opt-in: `InsightFixRouting` deterministically creates FixException runs for NewException AND ExceptionSpike insights (readiness-prechecked, system-authored — no userId) and findings-based FixPerformance runs for TraceLatencyRegression insights (precomputed `SpanTreeAnalyzer` findings + code locations via the refactored `createPerformanceFixTaskFromFindings`; the existing trace path is byte-identical); ErrorLogSpike/MetricDrift are never auto-fixed; every run reuses the enforced guardrails — `FixRunBudget` daily cap, per-repo `maxOpenFixPullRequests` token gate, draft PRs, per-subject/per-trace dedupe — and stamps `AIRun.triggeredBySentinelInsightId` provenance. **Stated plainly: this is NOT the "graduated finding types" gate this row demands.** Measured-precision graduation is still unimplemented — the gate is an explicit product-owner opt-in, not an earned precision threshold (see D8) — so the box stays unchecked until precision thresholds gate the automation)*
- [ ] Widen the fix context beyond the stack trace: fix tasks read the finding's cited evidence (logs/traces/spans/occurrence history) — the "grounded in full telemetry" differentiator, currently absent even from the manual path
- [ ] Prevented-incident accounting, conservatively defined (finding fixed → signal returned to band → no correlated incident within N days, Q10) — no public "incidents prevented" claim before this exists

Hard prerequisites: code-fix track design spike (sandbox) before any X-lane automation; G11 before any auto-created fix task; G1 circuit breaker before fix-task fan-out; G4 budgets (already in force) cover the LLM triage spend.

### Phase 4 — Memory, multi-agent, graduated autonomy

- [ ] Persisted episodic case store (root cause + citations + fix linkage) + similarity retrieval; recurrence short-circuit — under G10
- [ ] pgvector RAG + temporal knowledge graph over postmortems/runbooks/service descriptions — under G10
- [ ] Human-correction memory (edited updates, rejected hypotheses, fixed PRs) — provenance + review gates (G10)
- [ ] Orchestrator-worker parallel hypotheses (needs planner/synthesizer model routing; ~15× token fan-out needs G4 budgets)
- [ ] Runbook-authoring flywheel (human-review gated)
- [ ] Per-problem-type accuracy scoring gates AutoRun (G3 green); public Agent Accuracy scorecard
- [ ] Moonshot lane: reliability-debt backlog, community runbook/skill registry (signing + review, G10), "Sentinel watching Sentinel" public dashboard, enterprise packaging (SOC2, air-gapped, SSO, per-team RBAC/audit — needs G5/G8)

---

## 4. GTM lane (dated — paced to the Opsgenie wedge)

Opsgenie EOL is **April 5, 2027**; migrating teams choose destinations 6–18 months ahead, i.e. **through late 2026**. JSM+Rovo and incident.io are actively courting this cohort.

| Target | Deliverable |
|---|---|
| Q3 2026 | Sentinel user docs + changelog/blog for the shipped investigation engine (currently dark: flags default off, zero docs) |
| Q3 2026 | BYO-LLM / local-Ollama / air-gapped story published; demo asset for the flagship "watch it think" panel |
| Q4 2026 | **Opsgenie importer** (schedules, escalation policies, routing rules) — post-EOL data is deleted, so the importer must exist while there is still data to import |
| Q4 2026 | Rewrite the Opsgenie compare page around the AI-SRE story ("migrate to an on-call platform that diagnoses before it pages") — today it does not mention Sentinel (`Home/Utils/ProductCompare.ts`) |
| Q4 2026 | Phase 2 measurement plumbing live on our own production project → first internal MTTR/accuracy numbers |
| Q1 2027 | Alert-storm collapse + quiet mode hardened — the on-call displacement story is credible before the EOL deadline |
| Before any public number | The claim's instrumentation exists (no "30–60% MTTR cut" marketing before cohort attribution ships) |

### Packaging (decision needed — Open question Q4)

- Cloud: autonomous investigations consume metered tokens (`$0.02/1K` per `Home/Utils/Pricing.ts` today, with auto-recharge) — wake-on-every-alert on metered tokens is structurally the "tokenpocalypse" we attack Datadog for. Recommendation: **included monthly investigation quota per plan + BYO-key as the unlimited escape hatch**; per-feature spend caps (G4) are a prerequisite for any default-on.
- Self-host: free AI with your own key — unchanged, and the headline.
- AutoRun tier placement: Enterprise, and only behind G3.

---

## 5. Non-goals

- **MCP `ToolGenerator` tools (~155) will not be exposed to autonomous agents.** Autonomous runs see the curated read-only toolbox, period. (The original "unified behind one `ObservabilityTool` interface" framing is retired — isolation is the feature, not a gap.)
- **No big-bang cutover of the legacy AIAgent.** Absorption per the Phase 3 milestone; the standalone worker keeps running (with the race fixed) until `code_fix` replaces it.
- **No second *metric*-anomaly scheduler.** Incident-opening proactive detection (Phase 3) triggers off existing anomaly monitors, and the metric-band evaluator is never re-implemented elsewhere. The Preventive lane's new watchers (drift filter, spans baseline, log novelty, post-deploy watch) are *new* deterministic sensors under the lane's own rules — they extend the sensor surface; they do not duplicate it.
- **No public accuracy/MTTR marketing claims before their instrumentation exists.**
- **No page cancellation, ever.** The most Sentinel may do is delay-with-dead-man's-switch (G7).
- **No LLM in the watch loop.** The always-on preventive watchers are deterministic; the LLM engages per-finding under G4 budgets. "AI reads every log line" is the tokenpocalypse we attack Datadog for — we will not build it.
- **The preventive lane never pages.** Findings are a quiet inbox; only incidents/alerts page, and only through the existing notification pipeline.
- **No auto-merge of AI-authored PRs, ever.** Human review is the permanent safety boundary of fix-before-incident (vision §6: code-from-telemetry supply-chain risk).

## 6. Open questions

| # | Question | Owner | Blocks |
|---|---|---|---|
| Q1 | ~~Queue technology~~ **Answered 2026-07-10:** DB-claim pattern on `AIRun` rows. The AIRun row was already the source of truth (status/heartbeat/sweeper existed), the CAS pattern was proven twice (chat resume, AIAgent task claim), it adds no Redis dependency, and it works air-gapped. BullMQ remains available if fan-out throughput ever demands it | eng | ~~Phase 2~~ resolved |
| Q2 | ~~Alert investigation gating defaults~~ **Answered 2026-07-10:** severity floor defaults to the top two tiers (per-project override via Alert AI settings); per-monitor dedupe window = 30 min (constant, configurable later if asked for) | product+eng | ~~Phase 1 closeout~~ resolved |
| Q3 | ~~Structured confidence signal design~~ **Answered 2026-07-13: both.** A deterministic evidence floor over the run's server-minted citations (zero citations AND zero data-bearing tool calls → inconclusive, no LLM spent — prose cannot fake evidence that was never minted) and, only past the floor, one constrained classification call (grader idiom: temperature 0, 20-token cap, word-bounded token parse). The structured result `{confident, source}` lets each consumer pick its own fail direction when the classification fails: the workspace ping fails LOUDER (sends), the instrumentation-PR trigger fails toward NO PR | eng | ~~G6, Phase 2~~ resolved |
| Q4 | Cloud packaging: investigation quota sizes per plan; what happens at quota (pause vs degrade to on-demand)? | product | GTM, default-on |
| Q5 | Default-on criterion for new projects: which measured precision bar over how many graded runs? | product | activation |
| Q6 | `LlmLog` retention: should the hard-coded 3-day cloud delete become a configurable default that also covers self-host (which has none)? Which roles may read prompt previews? | eng+security | G8 |
| Q7 | Code-fix sandbox: build on existing Workflow/Worker infra vs dedicated runner pool vs external CI triggers? | eng | Code-fix track |
| Q8 | Log-novelty technique: Drain-style template mining in ClickHouse vs embedding clustering — storage + cost envelope, per-tenant cardinality limits | eng | Preventive lane (last watcher) |
| Q9 | SLO/error-budget as a product feature: burn-rate is the canonical preventive signal, and marketing already claims "SLO tracking / error budgets" that do not exist in the product (`Home/Views/solutions/sre.ejs`, `Home/Utils/ProductCompare.ts` — verified 2026-07-13). Build SLOs, or correct the pages? | product | Preventive-lane detectors; marketing honesty either way |
| Q10 | Prevented-incident metric: counterfactual window and correlation rule — what may we honestly count and publish? | product+eng | G11, preventive-lane GTM claims |

## 7. Deviations log

| Date | Deviation | Rationale / residual |
|---|---|---|
| 2026-07 | **D1:** Auto-postmortem (Phase 3) and recurrence memory v1 (Phase 4) shipped before Phase 2 | Thin slices on existing substrate ("no-new-storage cut"); residuals — durability (queue) and persisted case store — remain in their original phases |
| 2026-07 | **D2:** Investigations shipped pre-queue as inline detached promises | Accepted orphan-on-restart risk for the additive read-only feature; mitigated only by the 12-min Stale sweep (marks, never resumes). Queue is now the first Phase 2 item and blocks all new autonomous triggers |
| 2026-07 | **D3:** Phase 0 mutations shipped before the policy gateway, undo, and accuracy scoring — violating the original §6 invariants as written | §6 rewritten from present-tense invariants to gated commitments; Safety Gates table (§2) now tracks each gate honestly with its interim mitigation |
| 2026-07 | **D4:** Quiet mode v1 (Phase 2 item) shipped early as a prose-regex | Acceptable while it only gates a workspace ping and fails loud; G6 blocks anything more consequential. **Residual closed 2026-07-13:** the regex is replaced by the structured confidence signal (`ConfidenceSignal.ts`) |
| 2026-07 | **D6:** `get_service_dependencies` topology tool shipped then reverted next day | Product call: the service map is not production ready; an AI citing a wrong dependency edge is worse than no topology. Re-add from commit 13455808bf when the map matures |
| 2026-07 | **D5:** Roadmap restructured: original `AISentinelReliabilityBrain.md` split into Vision + this tracker | The single doc's execution claims were stale at merge time ("zero implementation" shipped in the same commit); vision content had not drifted at all |
| 2026-07 | **D7:** A tool-free AI workflow component shipped outside the phased Sentinel investigation/agent plan | Workflows need a bounded text-generation primitive, not an autonomous agent. The surface deliberately exposes no tools or provider credentials, sends only explicit inputs, treats free-form output as untrusted, and reuses the metered provider/budget substrate; structured-output control flow and agentic workflow actions remain future, separately gated work |
| 2026-07 | **D8:** Insight-driven auto-created fix tasks shipped 2026-07-14 behind the default-false `Project.enableInsightFixTasks` opt-in BEFORE G11's measured-precision graduation exists — a product-owner decision, not an earned threshold | Same posture as the ImproveInstrumentation carve-out, honestly tracked (D3 pattern): explicit default-false opt-in, every enforced guardrail applies (daily fix budget, per-repo PR cap, draft PRs, dedupe, readiness prechecks), and the confirm/dismiss precision measurement now accumulates on the insights themselves. Residual: precision thresholds/graduation logic must exist before any default-on or "graduated" claim — G11 stays 🟡 and blocks exactly that |

## 8. Changelog

- **2026-07-15** — **The AI Tasks page now renders live readiness instead of a static prerequisites banner — and the banner it replaced was wrong.** The page permanently displayed "AI needs a connected GitHub repository and a project LLM provider" to every project regardless of state. Two defects, one omission. **(1) "project" is false:** agent completions have been server-mediated since B4 Tier 0, so `getLlmProviderForMeteredAgentPath` resolves project-owned → shared global, and a project with no provider of its own is ready (the 2026-07-14 entry already recorded this for the readiness gate; the UI copy was never updated, and `Settings > AI Agents` additionally claimed "self-hosted agents always require a project LLM provider" — also false, self-hosted agents call the same metered endpoint). **(2) The agent gate was never mentioned:** `getAIFixReadiness` has always returned three checks, so a project with repo + LLM but no running container watched its tasks sit `Queued` with nothing on screen explaining why — the biggest honesty gap, and the reason the section exists. **(3) It was static:** it could not tell a configured project from an empty one. New `Common/Server/Utils/AI/CodeFix/CodeFixReadiness.ts` owns the three gates; `TelemetryExceptionService.getAIFixReadiness` now calls its llm/agent helpers and keeps only its per-exception `repositoryResolved` check, deleting the duplicated billing predicate — the two surfaces can no longer drift into telling the user different stories. New `POST /ai-readiness/code-fix` (`AIReadinessAPI`) answers the project-wide question; its repo gate is deliberately a **distinct id** (`repositoryConnected`, the weaker any-GitHub-App-repo claim from `SubjectCodeFixRun.hasGitHubAppConnectedRepository`) rather than reusing `repositoryResolved`, because a page-level check cannot promise any given exception's stack trace will match. Readiness types moved to `Common/Types/AI/AIFixReadiness.ts`; `ExceptionExplorer` imports them instead of hand-duplicating them. The `detail` field is now populated when `ok` too (`ExceptionExplorer` only renders it when `!ok`, so this is additive) and carries the **provider scope** — a project reading "Using X, a shared provider available to every project on this instance" can see the zero-config path is working without going to Settings. UI is a `Card` + `ProgressBar` + per-gate tiles matching `Components/Home/GettingStarted.tsx`; a ready project collapses to one line with a Details toggle rather than self-retiring, because readiness **regresses** (a container dies, a balance empties). Errors surface via `ErrorMessage` and are never hidden — a vanishing readiness section reads as "all clear", the one lie this component exists to prevent. **"GitHub" is kept: verified accurate**, not vestigial-GitLab hedging — `AIAgentDataAPI` hard-rejects every other `repositoryHostedAt`. Tests: 14 in `CodeFixReadiness.test.ts` (free-global ready with no project provider and no balance queried; costed-global + billing on + empty balance not ready; billing off skips the balance; gate ordering; each single gate failing alone), and the pre-existing 16 in `TelemetryExceptionAIFixReadiness.test.ts` pass unchanged through the shared helpers, which is what proves the refactor behavior-preserving. | `Common/Server/Utils/AI/CodeFix/CodeFixReadiness.ts`, `Common/Server/API/AIReadinessAPI.ts`, `Common/Types/AI/AIFixReadiness.ts`, `App/FeatureSet/Dashboard/src/Components/AIAgentTask/AICodeFixReadiness.tsx`, `Common/Server/Services/TelemetryExceptionService.ts`

- **2026-07-15** — **Code-fix runs are now a shared team artifact: readable by the whole project through the standard `/ai-run` CRUD, so the AI Tasks list is a plain `ModelTable<AIRun>`.** The AI Tasks pages were hand-rolling a table, pagination and stat tiles because `AIRunService`'s flat per-user pin (`query.userId = <caller>`) hid code-fix runs from the generic CRUD, forcing the bespoke `/code-fix-run/list`. The pin was protecting the wrong thing here: it is there for personal AI *chat*, but code-fix runs are triggered off an exception/incident, produce a pull request, and their child `AIAgentTaskPullRequest` rows were already project-readable through the standard CRUD. Worth naming: the in-tree comments justifying that design (`CodeFixRunAPI`, `CodeFixRunsTable`) asserted code-fix runs are "system-authored (userId = null)" — **false**; `TelemetryExceptionService` and `SubjectCodeFixRun` stamp the clicking user's id on all four human-triggered recipes, so the pin was hiding *other people's* fix runs while already surfacing the caller's own at full column width. New `Utils/AI/AIRunPrivacyFilter` forces `(runType = 'CodeFix' OR "AIRun"."userId" = :caller)` onto every non-root read, AND-ed on via `combineWithPrivacyClause` — modelled on `Utils/Incident/IncidentPrivacyFilter`. The clause is **built from server state and never reads `query.runType`**, which is load-bearing, not stylistic: `onBeforeFind` runs (`DatabaseService.ts:1573`) *before* `QueryUtil.serializeQuery`, so a dispatch keyed off the caller's value sees raw client operator objects — `NotEqual("CodeFix")` and `LessThan("CodeFix")` both stringify to exactly `"CodeFix"` but compile to `!=` / `<` (and `"Chat" < "CodeFix"` lexicographically), `MultiSearch(fields:[])` deletes the predicate outright, and `Includes(["CodeFix","Chat"])` is the shape `BaseModelTable`'s own dropdown emits — so accepting it *is* the exploit. Forced, a caller filter can only NARROW. Applied in **both** `onBeforeFind` and the `countBy` override (there is no `onBeforeCount` hook, and `BaseAPI.getList` issues both with the same query — filtering one alone yields a scoped list beside a project-wide count). Unchanged by construction: Chat runs (`userId = caller` only), Investigation runs (`runType != CodeFix`, `userId` null → still hidden, still read as root), and `pinQueryToRequestingUser` itself, which `AIConversation`/`AIConversationMessage`/`AIRunEvent` keep byte-identical. **`AIRunEvent` deliberately NOT relaxed** — it has no `runType` to key the clause off, and "null userId" does not mean system-authored (the investigation engine writes null-userId events too), so project-scoping it wholesale would expose other members' chat `toolArguments` (user-typed prompt content); the detail page's event trail therefore still goes through `/code-fix-run/get`, which authorizes the parent run first. `/code-fix-run/list` is deleted. `AIRun` gains `Permission.Viewer` on its table ACL and all 30 readable columns (matching Incident's convention) so read-only members keep the access the retired endpoint's Project-read gate gave them — `ReadProject`/`ProjectUser`/`UnAuthorizedSsoUser` are **not** added: that endpoint admitting them was an accident of gating on Project-read, not a designed grant. Migration `1784105912819-BackfillCodeFixTaskType` writes `codeFixTaskType = 'FixException'` on legacy nulls (scoped to `runType = 'CodeFix'`): a "Fix Exception" filter now compiles to `IN ('FixException')`, which NULL never matches, and the equalToOrNull the bespoke endpoint used to paper over that died with it. Tests: 20 in `AIRunPrivacyFilter.test.ts`, asserting on the **final serialized query** (post-`serializeQuery`, the only thing that proves the clause survives) across every hostile operator above plus the no-runType get-item case, the API-key-shaped no-userId rejection, the bound-parameter values, and a negative control proving the assertion can fail. | `Common/Server/Utils/AI/AIRunPrivacyFilter.ts`, `Common/Server/Services/AIRunService.ts`, `Common/Server/API/CodeFixRunAPI.ts`, `App/FeatureSet/Dashboard/src/Components/AIAgentTask/CodeFixRunsTable.tsx`

- **2026-07-14** — **Shipped the Preventive lane's first full slice: Sentinel Insights — deterministic watchers → quiet findings inbox → budgeted per-finding triage → opt-in draft-PR fixes (W-4/W-5 shipped, W-2 half-shipped, both F items, X-2, and the X-3 engine behind opt-in — see D8; **W-1 was attempted and reverted before push**, see its row).** Safety posture, enforced not aspired: **no LLM anywhere in the watch loop** (detectors are pure statistical checks over existing service methods), **insights never page and never open incidents** (nothing touches the notification pipeline), everything automatic sits behind **default-false Project flags**, fix PRs are drafts inside every enforced guardrail, and the G4 daily token budget covers all triage spend. Seven legs. (1) **Five detectors** (`Common/Server/Utils/AI/Sentinel/Insights/Detectors/`, house pattern: pure decision statics + thin IO, thresholds as named exported constants with rationale): **NewException** (fingerprint first seen ≤24h with ≥3 occurrences; High ≥50), **ExceptionSpike** (established fingerprints at ≥10 occurrences in the last hour AND ≥5× the prior-24h hourly rate — dormant awakening falls out of the same math; High ≥10×), **ErrorLogSpike** (project-wide Error+Fatal histogram, projection-fast: recent hour ≥100 AND ≥3× the prior-24h hourly average, then ≤3 per-service insights attributed inside the spike hour only; High ≥10×), **TraceLatencyRegression** (per-service p99 recent-1h vs prior-24h through the existing span projection — **p99 deliberately, p95 is not projection-served**; regression = ≥1s AND ≥2× prior AND ≥100 prior samples, High ≥4×; drills only on regression to one representative slow trace and stores `SpanTreeAnalyzer` findings + code locations as evidence that outlives span retention), **MetricDrift** (new `MetricBaselineService.getWeekOverWeekDrift` over the existing `MetricBaselineHourly` per-day rows: both windows ≥100 samples, |mean change| ≥50%, top 5, **always Low, never auto-fixed** — reads learned baselines, does not re-evaluate bands, per the non-goal). (2) **Scanner + store:** `SentinelInsight:ScanForInsights` worker every 15 min (10-min timeout < tick = no overlap), `enableSentinelInsights=true` projects only, per-project AND per-detector failure isolation — a scan never throws into the cron harness; `InsightStore` dedupes on the most recent (projectId, fingerprint) row: non-terminal rows refresh lastSeenAt/occurrenceCount/severity/evidence (status never touched), a human Dismiss suppresses recreation for 7 days (inclusive boundary, noise-averse), Resolved may recreate (a reappearing issue is a regression), new insights capped at **10/project/scan** with dropped fingerprints logged — no silent caps. (3) **`SentinelInsight` model + lifecycle:** fingerprint/firstSeenAt/lastSeenAt/occurrenceCount, Detected → ActionRequired|FixOpened → Resolved|Dismissed, `humanVerdict`/`humanVerdictAt`/`humanVerdictByUserId` (Confirmed|Dismissed) — the G11 measurement lives on the finding itself from day one; hand-written migrations (CREATE TABLE + Project/AIRun column adds), registered. (4) **Triage:** every new insight enqueues a read-only Investigation AIRun on the existing durable queue (new `AIRun.triggeredBySentinelInsightId` subject; `InvestigationQueue.enqueue` now returns the run id so the insight links it; CAS claims/retries/TTL/stale sweeper inherited with zero changes), budgeted under new AUTONOMOUS feature **"Sentinel Insight Triage"**, deduped to one non-terminal run per insight, quiet-skipped without provider or budget; `InsightTriageRunner` frames the preventive persona (no incident exists — root cause, blast radius, ONE next action, cite evidence) and postAnalysis writes ONLY `triageSummaryMarkdown`/`triageCompletedAt` — no feed items, no notifications, no instrumentation trigger. (5) **Fix routing — deterministic, never LLM-decided:** strictly behind default-false `Project.enableInsightFixTasks`; NewException/ExceptionSpike → readiness-prechecked, system-authored FixException runs; TraceLatencyRegression → findings-based FixPerformance via the refactored `createPerformanceFixTaskFromFindings` (existing trace path byte-identical); ErrorLogSpike/MetricDrift never auto-fix; every run inside `FixRunBudget` + the per-repo PR cap + draft PRs + dedupe, provenance-stamped `triggeredBySentinelInsightId`; fix created → FixOpened + `fixAiRunId`, else ActionRequired; budget/readiness misses are logged quiet-skips, and routing never throws into the scanner. (6) **API + dashboard:** `POST /sentinel-insight/verdict|resolve|triage-run` (user-props access check, then root service work; mounted before the generic CRUD router so the action routes win) + Sentinel nav → **Insights** (`/dashboard/:projectId/ai/insights`): filterable inbox (type/severity/status/service), detail page with evidence markdown, live triage panel (5s poll, `AIRunEvent` activity feed), fix-run link, one-click Confirm/Dismiss/Resolve with optimistic rollback, settings page with the two toggles. (7) **Adversarial review before push — 8 confirmed findings fixed in the same slice** (4 dimension finders → per-finding refuting verifiers; 2 findings refuted, 8 confirmed and fixed, and one "fix" deleted as a fiction): **(a)** `ErrorLogSpike` parsed ClickHouse's zone-less `DateTime` buckets with `new Date(...)`, i.e. as worker-LOCAL time — on a non-UTC worker a positive offset silently suppressed every spike and a negative one fabricated them; now normalized to UTC through a pure `parseBucketTime` (anchored zone-less regex, zone-carrying strings pass through), with the suite TZ-pinned to `Asia/Kolkata` (positive AND :30 offset) and mutation-tested to prove the old ISO-Z fixtures could never have caught it. **(b)** `TraceLatencyRegression` stage 1 was a 25h raw-span scan per project per tick (the `getAnalyticsTable` path is projection-disqualified); it now rides `proj_agg_by_service` through a new additive `TraceAggregationService.getServiceLatencyProfile` (count + p99 states merged across minutes — a subset group-by, the `getResourceFacetCounts` trick; ns→ms in TS so the aggregate byte-matches the stored one), and — because `timeout_overflow_mode='break'` returns partial aggregates with no exception and no marker — the method reports `isPartial` (elapsed ≥90% of the 45s cap, or an unparseable body) and the detector **skips the project for that tick rather than filing an insight from possibly-truncated data**. **(c)** the drill picked the "slowest operation" by absolute p99 with no sample floor, so a single one-off slow span (n=1, its own duration IS the p99) could win and — since the fingerprint embeds the operation name — churn the dedupe key every tick, filing a fresh insight + triage run for one ongoing regression; now the winner must clear a 20-sample floor with a deterministic total-order tie-break, falling back to stable service-level evidence when nothing clears it. **(d)** fix routing ignored the `Project.enableAi` master kill switch (the scanner even selected the flag and never read it) — a project with AI off could still get autonomous draft PRs; the master switch now gates routing ahead of the opt-in. **(e)** insight triage runs share `runType: Investigation` with incident/alert RCA and could saturate the per-project concurrency cap, delaying the human-facing RCA the product promises; the preventive lane is now sub-capped (`INSIGHT_TRIAGE_RESERVED_SLOTS`, floored so a cap-1 project still triages) so an interactive run always finds a slot. **(f)** an insight created but not routed (pod death or a failed status write between the two writes) was stranded in `Detected` forever — refreshes never touch status, so it was never triaged, never fix-routed, and its fingerprint was permanently pinned; a self-heal sweep now re-routes stranded rows at the top of each project's scan (safe by construction: fix creation dedupes per-(exception, recipe)/per-trace and triage dedupes per insight). **(g)** the insight detail page rendered detector evidence AND the LLM triage summary through `MarkdownViewer` without `safeMode`, so a markdown image injected into an *exception message* (interpolated raw by the exception detectors) became a zero-click tracking beacon in a privileged member's browser — both call sites now use `safeMode`, the repo's established defense for this content class (`SafeChatMarkdown`). Not fixed, recorded honestly: `safeMode` has **zero test coverage repo-wide** (`react-markdown` is mocked out in the only suite with a DOM). **(h)** **the W-1 "fix" was reverted, not shipped** — see the W-1 row: `resolveSeriesPrimaryEntityId` was inert (the monitor pipeline carries no entity identity, and the MV's entity dimension does not correspond to the monitor's attribute dimension), and its 10 green tests asserted a row shape no producer emits. Deleted rather than shipped as a false claim. Tests: **213 across 17 suites** (`Common/Tests/Server/Utils/AI/Insights/` + `SentinelInsightService.test.ts` + `TraceServiceLatencyProfile.test.ts`) — pure threshold matrices at exact boundaries, store dedupe/cooldown/cap matrix, scanner gating + isolation + never-throws, fix-routing eligibility matrix, triage gates, queue subject dispatch, verdict/resolve semantics, W-1 regression, FixPerformance refactor invariance. Honest residuals: **no measured-precision graduation** — the automation gate is an explicit product-owner opt-in, not an earned threshold (D8; the tracker must not pretend otherwise); release-change signal (W-3) and log-novelty (W-6, Q8) not attempted; drift is an insight source only, not a monitor criteria filter; thresholds are conservative fixed constants (no per-project tuning); latency evidence is one representative trace (no cross-trace frequency); evidence is captured as-of-detection because ClickHouse retention is shorter than an insight's life — re-reading the raw telemetry later may find it gone; ErrorLogSpike attribution caps at 3 services; triage summaries land quietly with no digest surface, by design.

- **2026-07-14** — **Shipped the tool-free Generate Text with AI workflow component.** One bounded completion uses the configured project-default/global-fallback provider through `AIService.executeWithLogging` (feature `Workflow AI`): project/plan AI access, global-provider billing, the daily autonomous token budget, and a three-call per-project concurrency cap are enforced; no provider key or endpoint is accepted as a workflow argument. The component's egress is explicit — a fixed safety instruction plus resolved System Instructions, Prompt, and optional JSON Context only — and it exposes no tools, so the model cannot query or mutate OneUptime. System Instructions, Prompt, Context, and Response are redacted from the AI component's own `WorkflowLog` entries (downstream reuse follows the receiving component's logging policy); prompt/response previews are disabled in `LlmLog`; raw provider error bodies are suppressed before application logging and span capture; provider, model, total/completion tokens, LLM Log ID, and safe errors remain available. Built-in bounds: 50,000 combined input characters, temperature 0–1 (default 0.2), 1–4,096 output tokens (default 1,024), one provider attempt, and a 60-second request timeout; Success/Error ports make provider, validation, budget, billing, concurrency, and timeout failures explicit. English workflow docs plus component help shipped with the surface. This is intentionally not a Sentinel agent: free-form output is untrusted and cannot invoke tools; structured-output branching and agentic actions are not claimed.
- **2026-07-14** — **Deleted the OpenCode fallback and the raw-key protocol — the in-house metered agent is now the only code agent, and no code path hands an LLM provider key to the worker.** The one-release grace window B4 Tier 0 promised is closed, one day after it opened. Removed end to end: `OpenCodeAgent` (+ its `CodeAgentType.OpenCode` enum member, factory case, and `CODE_AGENT_TYPE=OpenCode` selection — an unknown `CODE_AGENT_TYPE` now warns and uses the in-house agent), the OpenCode CLI install in `AIAgent/Dockerfile.tpl` (worker image no longer curls opencode.ai at build), the `POST /ai-agent-data/get-llm-config` endpoint, `BackendAPI.getLLMConfig` + the `LLMConfig` wire types, the raw-key fields on `CodeAgentLLMConfig` (now just a required `taskId`), the llmConfig plumbing in both PR pipelines, and `LlmProviderService.getLlmProviderForAgentTasks` (the raw-key resolver with the cloud project-owned-only restriction). Consequence worth naming: `TelemetryExceptionService.getAIFixReadiness` now resolves through `getLlmProviderForMeteredAgentPath`, so the llmProvider readiness check passes on cloud with NO project-owned provider (the metered global provider serves it) — cloud zero-config is now true at the readiness gate too, and the checklist message no longer claims project-owned-only. That switch alone would have opened a fail-late hole (a costed global provider bills the project's AI balance per completion, so a zero-balance cloud project would pass a presence-only readiness check and error at the run's FIRST llm-completion call), so the llmProvider check now also models PAYABILITY: when billing is on and the resolved provider is global+costed, readiness verifies `Project.aiCurrentBalanceInUSDCents > 0` (mirroring `AIService.executeWithLogging`'s billing gate exactly — project-owned and free-global providers never require a balance) and fails with recharge-or-BYO guidance. G5's worker-egress claim (git push + OneUptime API only) is now unconditional. Tests: the `getLlmProviderForAgentTasks` describe block is deleted from `LlmProviderProjectOwned.test.ts` (the metered-path suite is the resolver's only contract now); readiness/code-fix-run suites spy on the metered resolver; new balance-gate suite in `TelemetryExceptionAIFixReadiness.test.ts` (zero-balance fails with recharge guidance, positive balance ready, free-global/self-host/project-owned never query the balance).
- **2026-07-13** — **Shipped B4 Tier 1: fix PRs are now CI-verified through the customer's own CI — the verified-green rate exists, and should-fail regression tests report their failure as the desired signal.** No sandbox (that's Tier 2): the fix PRs are already drafts, the repository's own CI runs them, and we READ the check-run conclusions — never re-run, never gate. Four legs. (1) **`ciStatus` on the PR row:** `AIAgentTaskPullRequest` gains nullable `ciStatus` (new enum `FixPullRequestCiStatus`: Pending / Green / Red / ExpectedFailureObserved / NoCiConfigured) + `ciStatusAt`; roll-up per head branch, in deliberate order — no check runs → `NoCiConfigured`, any pending → `Pending` (conclusions are FINAL-only: a failure observed while other checks still run is not yet the branch's conclusion), any failed → `Red`, else `Green`; success/neutral/skipped count as passing, EVERYTHING else (timed_out, cancelled, action_required, stale, unknown future values) counts as failed — per G9, unknown fails toward unverified, never verified. (2) **Polling rides the existing sweep:** `AIAgent:SyncPullRequestStates` (same 30-min cadence, same one-token-per-installation cache) now mints its token with `checks: read` added and fetches `GET commits/{headRef}/check-runs` per open PR (`GitHubUtil.getCheckRunsConclusion[WithToken]`, paginated, filter=latest semantics = what a reviewer sees); an App installation WITHOUT the "Checks: Read-only" permission degrades gracefully — token re-minted without checks, PR-state sync never regresses, ciStatus stays null (= unverified); CI-fetch failures never block the state update; the conclusion is also captured on the sweep where a PR leaves Open, so merged PRs carry their final verdict. On every ciStatus CHANGE the run's glass-box trail gets a `ProgressLog` event ("CI on the fix PR: Green (4/4 checks passed)" / "Regression-test PR: expected failure observed"). (3) **The SHOULD-FAIL rule, deterministic:** when the PR's run (joined via `aiRunId`, recipe looked up lazily and only on Red) is `WriteRegressionTest`, RED maps to `ExpectedFailureObserved` — the failing check IS the desired signal for a test that must fail until the bug is fixed, closing B2's "nothing verifies the generated test actually fails" residual. Honest caveat, in code and here: check-run granularity cannot tell WHICH job failed — a repo whose lint breaks on the PR also reads as expected-failure; job-level discrimination (inspect check names) is the tracked refinement. (4) **Verified-green rate:** `getOutcomeStats` gains `verifiedGreen` (merged PRs whose last ciStatus was Green/ExpectedFailureObserved — the ONLY two conclusions `FixPullRequestCiStatusHelper.isVerified` accepts, G9) and `verifiedGreenRatePercent` (over MERGED, null only when nothing merged — the acceptance-rate honesty rule; merged PRs with no CI, null, Pending or Red drag the rate DOWN, they are never excluded from the denominator); endpoint + outcomes card gain a fifth "CI-verified merges" tile (em-dash + "No merged PRs yet" on null). Residuals, tracked honestly: job-level SHOULD-FAIL discrimination (above); 30-min polling freshness (a PR merged seconds after CI finishes waits a sweep for its verdict; webhook-driven `check_suite` events are the refinement); the GitHub App needs "Checks: Read-only" granted (an operator action in GitHub App settings — cloud and self-host both; until granted the sweep degrades to PR-state-only, logged); no per-check-name surface in the UI (one rolled-up value only). Tests: roll-up matrix + SHOULD-FAIL mapping + G9 verified-predicate pins (`FixPullRequestCiStatus.test.ts`), check-run JSON counting incl. unknown-conclusion-fails-closed (`GitHubCheckRunsConclusion.test.ts`), verified-green stats math incl. independent denominators and rate-0-not-null (`AIAgentTaskPullRequestOutcomeStats.test.ts`). **Migration required:** two nullable `AIAgentTaskPullRequest` columns (entity edited; generate via `npm run generate-postgres-migration`).

- **2026-07-13** — **Shipped B4 Tier 0: the in-house metered code agent — the OpenCode CLI shell-out is no longer the default, and the worker no longer holds LLM provider keys.** Three legs. (1) **Server completion endpoint** `POST /ai-agent-data/llm-completion` (agent-key auth like its neighbors): validates the run is a claimed **Running CodeFix** run owned by the calling agent (`aiAgentId` match), resolves the provider via new `LlmProviderService.getLlmProviderForMeteredAgentPath` (project-owned first, else the global provider **on cloud too** — this path is metered through `AIService.executeWithLogging`, feature **"Sentinel Code Fix"**, added to `AUTONOMOUS_AI_FEATURES` so the G4 daily budget covers it; `aiRunId`-linked, `storeContentPreviews: false`), and enforces **server-side per-run loop budgets**: max **40 completion calls** and **100k output tokens**, counted from the run's own LlmLog rows (new `LlmLog.completionTokens` column + migration; one row per call, success or error — restart-proof, not trusted to the worker); over-budget → 4xx with a clear message; each call refreshes the run heartbeat. Response is `{message:{role,content,toolCalls}, stopReason, budget:{completionCallsUsed,maxCompletionCalls,outputTokensUsed,maxOutputTokens}}` so the worker can wind down to an honest final summary before the wall. The Workstream-A restriction lifts **the right way**: metering is what forbade the global provider on the agent path, and this path meters — cloud zero-config completes (fix tasks work with no project-owned provider; usage billed as metered AI tokens). The OLD raw-key resolution (`getLlmProviderForAgentTasks` / `get-llm-config`) keeps its cloud restriction untouched until removed. (2) **In-house agent** `AIAgent/CodeAgents/InHouseCodeAgent.ts` (implements the same `CodeAgent` interface): a tool loop driven entirely by the completion endpoint — system prompt = the recipe prompt (unchanged from the handlers) + tool-use contract; tools `read_file`/`write_file`/`list_directory`/`search_files` (all path-guarded to the workspace via new pure `CodeAgentWorkspaceGuard.resolveWorkspacePath` — `..`, absolute-outside and sibling-prefix escapes refused) and `run_command` (workspace cwd, 120s timeout, combined output; ALL tool outputs truncated to 20k chars with an explicit marker); malformed tool-call JSON is refused, never executed with empty args; every tool call lands as a `ProgressLog` event on the run's glass-box trail ("read src/…", "ran npm test (exit 1)") via the existing TaskLogger route; modified files detected via `git status --porcelain` as before; worker winds down on the server-reported budget (forced no-tools final turn) so runs end with a summary, not a 4xx. (3) **Factory + fallback:** `CodeAgentFactory` defaults to InHouse; `CODE_AGENT_TYPE=OpenCode` selects the legacy shell-out **for one release** — `OpenCodeAgent`, `get-llm-config` and `BackendAPI.getLLMConfig` are untouched but carry deprecation comments pointing at the design doc; both PR pipelines fetch the raw LLM config ONLY on that fallback. G5 improves: the fix-path worker now holds no provider secret (its remaining egress = git push + the OneUptime API). `run_command` retains the status-quo risk OpenCode already had (repo's own scripts run in the worker container) — documented, Tier 2's job. Docs: `ai-agent.md` + `llm-provider.md` cloud-provider sections rewritten (global provider now works on cloud, metered; loop budgets stated). Tests: `CodeFixAgentCompletion` guard matrix (wrong agent / non-Running / non-CodeFix / over-budget calls / over-budget tokens / happy path pinning feature+redaction+budget shape), metered vs raw-key provider-resolution split in `LlmProviderProjectOwned.test.ts`, and the pure worker edges (`CodeAgentWorkspaceGuard`: escape matrix + truncation) in `CodeAgentWorkspaceGuard.test.ts`. Residuals, tracked honestly: OpenCode fallback + `get-llm-config` still exist for one release; no build/test verify loop yet (Tier 1 = B4-PR3); `search_files` is `git grep` (tracked files only).

- **2026-07-13** — **B4 design spike shipped and approved for implementation** ([CodeFixSandboxDesign.md](./CodeFixSandboxDesign.md)): Tier 0 = in-house tool-loop agent with SERVER-MEDIATED metered completions (kills the raw-key handoff; re-enables the global provider on cloud — zero-config completes), Tier 1 = verification via the customer's own CI on draft PRs (check-run polling, verified-green rate, deterministic SHOULD-FAIL reporting for regression tests), Tier 2 = ephemeral egress-denied gVisor sandbox, deferred until observed demand. Docs-site sidebar + Home/ marketing pages join the Sentinel brand in the same PR.

- **2026-07-13** — **Shipped the G3 eval-harness bootstrap: labeled recorded runs are now scorable — corpus export + four-axis scoring, no LLM re-run; full offline replay explicitly documented as open.** The Phase 2 measurement columns (`AIRun.humanVerdict`/`autoGrade`) plus the glass-box `AIRunEvent` trail make the roadmap's four scores computable from what production already recorded, so the bootstrap ships that half honestly instead of waiting for replay. New `Common/Server/Utils/AI/Eval/`: (1) **`EvalCorpus.ts`** — `GoldenCase` JSON shape (runId/projectId/subjectType/completedAt, `label` {humanVerdict?, autoGrade?}, deterministic per-run `confidence` stats {citationsMinted, dataBearingToolCalls, toolCallsTotal, toolCallsFailed} derived from the event trail, `analysisPosted`, skeletal `events` [{sequence, eventType, toolName, ok}]); `exportCorpus` selects completed Investigation runs that HAVE a label (unlabeled runs carry no ground truth — excluded; the humanVerdict-OR-autoGrade select is two indexed legs unioned by id, newest first). "Data-bearing" is defined ONCE: new exported `SentinelConfidenceSignal.isDataBearingRowCount` (rowCount > 0; rowCount 0 stays proof-of-absence evidence), reused by both the live floor and the corpus derivation so the two can never drift; `analysisPosted` = the subject's RootCause feed item exists (the InvestigationGrader sourcing rule — subject-level, honestly noted, since feed items are not keyed by run). (2) **`EvalScores.ts`** (pure) — `topHypothesisPrecision` = autoGrade Match/(Match+Partial+Mismatch) and `humanConfirmedRate` = Confirmed/(Confirmed+Rejected) **reported separately, never blended** (different instruments, different biases); `citationGroundingRate` = posted analyses backed by ≥1 server-minted citation; `toolSelectionAccuracy` = Σ data-bearing / Σ finished tool calls (aggregated, not mean-of-per-run-rates — tiny runs must not dominate); `inconclusiveRecall` = among human-REJECTED cases, the fraction the deterministic evidence floor would have flagged on its own (reuses the floor's `hasVerifiableEvidence`, so the offline answer matches what production would have decided). Every score is `{value, numerator, denominator}` with **value null when the denominator is 0** — the acceptance-rate honesty rule, no fake 0s or 100s. (3) **`npm run ai-eval`** → `Scripts/AI/RunInvestigationEval.ts` (the generate-postgres-migration invocation pattern: config.env sourced, host-exposed Postgres localhost:5400, plus `RUN_DATABASE_MIGRATIONS_ON_BOOT=false` so a read-only eval can never migrate schema; `--project=<id>`/`--limit=<n>` args): prints the report table and writes `./eval-corpus.json` (gitignored); **below 20 labeled cases the report shouts that the numbers are noise** — `sampleTooSmall` is part of the report shape so no downstream consumer can miss it. (4) **`ReplayInvestigation.ts`** — a typed NOT-IMPLEMENTED skeleton making the replay gap explicit: verified against both emitters, `AIRunEvent.resultSummary` carries ONLY rowCount/durationInMs/isTruncated/bytesSentToLlm/errorMessage — the serialized tool output the LLM actually read is persisted NOWHERE (investigations set `storeContentPreviews: false` by design, G8), so "replay" today could only re-query live telemetry (retention-decayed, changed — not replay). Full replay needs: recorded redacted tool-result payloads per ToolCallCompleted (G8 access/retention review applies), a recorded-result tool transport with divergence counting, and pinned model config; recording is the prerequisite that only pays off for runs recorded after it ships — hence corpus/scoring first. G3 row updated (measurement half exists; corpus size + thresholds + replay still block); the Phase 2 item stays unchecked with the shipped half annotated. Tests: `EvalScores.test.ts` (every score's numerator/denominator/null rules, no-blending, aggregation semantics, floor-recall, small-sample boundary), `EvalCorpus.test.ts` (stat derivation from synthetic trails: proof-of-absence vs data-bearing, ToolCallStarted excluded, failed-call accounting, event projection; pins the shared data-bearing helper).

- **2026-07-13** — **Shipped the Preventive-lane X fix-pipeline guardrails + removed the legacy AIAgentTask tables.** Four legs. (1) **Per-project daily fix-run budget:** new nullable `Project.aiDailyFixTaskLimit` (null = default **25**/UTC-day — deliberately NOT unlimited, unlike the token budget: fix runs open PRs on customer repos; 0 = paused, the token-budget kill-switch semantics; setting on both AI settings pages beside the token limit). Enforced centrally by `Common/Server/Utils/AI/CodeFix/FixRunBudget.ts` (countBy of the project's CodeFix AIRuns created since UTC midnight — ALL statuses and recipes count) at BOTH creation paths: `createCodeFixRunForException` (exception recipes) and `enqueueSubjectCodeFixRun` (subject/perf recipes), so no recipe or future trigger can route around it. User-triggered paths get a clear BadDataException naming the setting; the automatic ImproveInstrumentation trigger pre-checks the budget and treats over-budget as a logged debug SKIP — never an error thrown into an investigation (its never-throws wrapper remains the backstop). (2) **Per-repo open-PR cap, enforced at the token gate:** new nullable `CodeRepository.maxOpenFixPullRequests` (null = default **5**, 0 = blocked; setting on the repo Settings page) — `get-repository-token` counts the repo's Open `AIAgentTaskPullRequest` rows (kept current by SyncPullRequestStates, so merging/closing frees a slot within 30 min) and refuses the installation token at/over cap with a message naming the cap and the setting. This is the hard enforcement point: **no token, no clone, no push, no PR** — the worker surfaces the message as run-failure guidance. (3) **Draft PRs by default:** `PullRequestCreator.createPullRequest` now opens every fix PR as a GitHub draft (all five recipes inherit it through the two shared pipelines), with a graceful fallback — a 422 naming drafts (private repos without the feature) logs and retries non-draft once; other errors never trigger the retry. PR-body boilerplate updated: every "review before merging" note now says the PR is a draft to be marked ready after review. (4) **Legacy AIAgentTask removal + agent registration hardening:** the `AIAgentTask`/`AIAgentTaskLog`/`AIAgentTaskTelemetryException` tables die — models, services, CRUD registrations, Models/Services indexes, the `*AIAgentTaskTelemetryException` permissions (the `*ProjectAIAgentTask` permissions survive: they govern the staying `AIAgentTaskPullRequest`), `AIAgentTaskPullRequest.aiAgentTaskId` (+ relation), the `AIAgentTaskType`/`AIAgentTaskMetadata` types, and the two legacy sweepers (`AIAgent:TimeoutStuckTasks`, `AIAgent:FailOrphanedScheduledTasks`) — the CodeFix orphan sweeper moved to its own aptly-named `FailOrphanedQueuedCodeFixRuns.ts`. `AIAgentTaskAPI`/`AIAgentTaskLogAPI` remain as the worker's HTTP protocol (routes/wire shapes unchanged, incl. the `AIAgentTaskStatus` wire strings) but are plain routers now, no CRUD model behind them. Historical task rows are dropped with the tables; their pull requests survive on `AIAgentTaskPullRequest` (aiRunId links the AIRun-era ones). Drop migration hand-written (typeorm cannot generate drops for deleted entities). And `Register.registerAIAgent` now retries FOREVER with capped exponential backoff (30s doubling to 5 min) instead of 10×30s-then-give-up; the non-clustered missing-AI_AGENT_ID `process.exit` became a thrown-and-retried error so the misconfiguration stays visible in logs instead of crash-looping. G11 moves ❌→🟡: caps + drafts + dedupe are enforced; measured precision gating, CI verify, and the findings inbox keep it from green. Tests: `FixRunBudget.test.ts` (pure decision matrix: default/custom/paused/negative/over/under + IO wiring incl. paused short-circuit), `OpenPullRequestCap.test.ts` (same shape for the token-gate cap, incl. at-cap-rejects and Open-state-only counting). **Migrations required:** generated add-columns for the two new settings + the hand-written legacy-table drop.

- **2026-07-13** — **Shipped G6: the structured, server-verified confidence signal — no investigation control flow derives from free-form prose anymore.** The `INCONCLUSIVE_RE` regex over the analysis (forgeable by prompt injection via telemetry) is deleted; new `Common/Server/Utils/AI/Sentinel/ConfidenceSignal.ts` computes a two-part signal after every completed investigation (Q3 answered: **both**). (1) **Deterministic evidence floor** (pure, no LLM): derived from the run's own server-minted citations — zero citations AND zero data-bearing tool calls → INCONCLUSIVE regardless of anything the prose claims, because citations are minted server-side from validated tool calls and prose cannot fake evidence that was never minted; the floor short-circuits with no classification spend. (2) **Constrained classification** (only past the floor): ONE metered call — new feature "Sentinel Confidence Classification", added to `AUTONOMOUS_AI_FEATURES` so the G4 daily budget covers it; `storeContentPreviews: false`, temperature 0, maxTokens 20, analysis truncated to 8000 chars, word-bounded CONFIDENT/INCONCLUSIVE parse (both/neither tokens → failed) — the exact `InvestigationGrader` idiom. The result is structured — `{confident, source: "deterministic-floor"|"classification"|"classification-failed"}` — so each consumer picks its own fail direction when the classification errs: the **workspace ping treats classification-failed as CONFIDENT** (quiet mode fails LOUDER, never silent — vision §6), while the **ImproveInstrumentation trigger treats it as NOT-inconclusive** (autonomous PR creation requires a POSITIVE inconclusive verdict — fails toward doing nothing, G11 posture); decisions route through `shouldSendWorkspaceNotification` / `shouldEnqueueInstrumentationTask`, never the raw boolean. Both runners' `postAnalysis` consume the signal; the engine's `InvestigationRequest` contract passes `confidence` instead of `isConfident`; the persona's literal-phrase contract ("Inconclusive — insufficient signal") is dropped — the model now just says it plainly. **Budget accounting note:** the classification fires AFTER the agent loop and the won Completed transition, so it sits OUTSIDE the per-run caps (8 LLM calls / 2000 output tokens govern the loop; `AIRun.llmCallCount`/`totalTokens` do not include it) but INSIDE LlmLog metering (linked via `aiRunId`) and the daily autonomous budget — a budget rejection degrades to classification-failed, never a run failure, and a falsely-requeued duplicate attempt can never double-spend it. G6 marked MET for every shipped consumer (future HypothesisBoard labels / page-delay must adopt the same pattern); D4 residual closed; docs updated (quiet-mode mechanism + per-run budget footnote). Tests: `SentinelConfidenceSignal.test.ts` (floor rules, token-parse edges incl. word-boundary traps, the per-consumer fail-direction table, no-LLM-call-below-floor, truncation, never-throws, budget-feature membership).

- **2026-07-13** — **Shipped Phase 2 measurement plumbing (server half): the accuracy/MTTR instruments the vision §8 metrics and G3 need, capturing signal from day one.** Three legs. (1) **Human verdict:** `AIRun` gains `humanVerdict` (new enum `AIRunHumanVerdict`: Confirmed/Rejected — deliberately two buttons; the roadmap's "edited" is detected by the grading side, not declared by a human), `humanVerdictAt`, `humanVerdictByUserId`. New `POST /ai-investigation/verdict` (user auth + subject access check, the panel's existing route idiom; body `{subjectType, subjectId, verdict}`) applies the verdict to the LATEST COMPLETED investigation for the subject via `AIRunService.applyHumanVerdictToLatestInvestigation`; overwrite allowed (users change their minds); rejected when no completed investigation exists; the panel-poll payload now carries `humanVerdict`/`autoGrade` state. (2) **On-resolve auto-grading:** new `InvestigationGrader` fires alongside the postmortem runner in `IncidentStateTimelineService` (fire-and-forget, never blocks resolve): when a completed investigation exists AND the incident has a non-empty human-recorded `rootCause`, ONE constrained LLM call (new feature "Sentinel Investigation Grading", added to `AUTONOMOUS_AI_FEATURES` so the G4 daily budget covers it; `storeContentPreviews: false`) compares the posted analysis (the RootCause feed item — same source the fix recipes read) with the final root cause and stores `AIRun.autoGrade` (new enum Match/Partial/Mismatch) + `autoGradeAt`. Parse is defensive (word-bounded single-token search — MISMATCH cannot double-count as MATCH; ambiguous/absent → no grade + warn); dedupe = autoGrade-already-set; **incident-only for now** (alerts have a `rootCause` column but no human resolution flow reliably populates it). (3) **Metrics:** new `IncidentMetricType.TimeToRootCausePosted` (`oneuptime.incident.time-to-rca`, seconds from incident creation to the RCA post) recorded best-effort from `IncidentInvestigationRunner.postAnalysis` — the shared attribute shape is factored into `IncidentService.getIncidentMetricContext` so it matches every other incident metric, and it is deliberately outside `refreshIncidentMetrics`' replace-list so refreshes never tombstone it; MTTA/MTTR (`TimeToAcknowledge`/`TimeToResolve`) gain an `aiInvestigated` attribute (one indexed `countBy` of completed investigations per refresh). Tests: `AIRunHumanVerdict.test.ts` (latest-completed targeting, reject-when-none, overwrite semantics), `InvestigationGrader.test.ts` (gate, token parse incl. the MISMATCH trap, skip/store/swallow paths). **Migration required:** five nullable `AIRun` columns (entity edited; generate via `npm run generate-postgres-migration`). The before/after MTTR *report* remains open — it needs accumulated data, not more plumbing.

- **2026-07-13** — **D1 DECIDED and shipped: Sentinel is the user-facing brand.** Founder decision (verbatim intent: "Sentinel becomes the user-facing brand"). Pass 1 renames user-visible strings across the dashboard — header "Ask Sentinel", nav category "Sentinel" (Chat / Tasks / Code Repositories), chat page + panel, "Sentinel Investigation" panel, Sentinel Tasks section, exception-page task cards, settings section label — with the repo's English-in-all-16-locales convention. Deliberately unchanged: code identifiers (PageMap.AI_COPILOT etc. — reviewability), billing term "AI Credits", runner-infrastructure copy ("agent container"), and the Home/ marketing pages (folded into the Q4 Opsgenie compare-page rewrite already in the GTM lane). The vision doc's "codename: Sentinel" framing is now simply "the brand".

- **2026-07-13** — **Shipped Workstream B5: the FixPerformance recipe — from a slow trace, one click opens a performance-fix PR grounded in span-tree evidence. This completes the user-approved recipe set (regression-test, instrumentation, RCA-fix, perf-fix; the synthetic-script fixer was explicitly skipped by user decision).** The design inverts the usual order: **deterministic evidence first, LLM second.** A new pure util `SpanTreeAnalyzer` (`Common/Server/Utils/AI/PerfEvidence/`) analyzes the trace's span tree with NO LLM and hard thresholds: (a) **N+1** — ≥5 sibling spans under one parent with near-identical names (digits/uuids/hex normalized) and, when `db.statement` is present, near-identical normalized statements; (b) **dominant slow span** — a span whose SELF time (duration minus child coverage — otherwise the root would trivially dominate) is ≥60% of the trace; (c) **sequential-that-could-be-parallel** — ≥3 same-name siblings running strictly one-after-another at ≥50% of their parent (suppressed when the same group already fired as N+1 — batching subsumes parallelizing). Trigger: a quiet **"Fix performance with AI"** button on the Trace Explorer → `POST /ai-investigation/create-performance-fix-task` (spans loaded under the USER's telemetry-read ACL — the access check IS the span query); **no finding → the request is rejected with "no deterministic performance pattern found", and that rejection is a feature — the button never pretends.** Human-triggered = no opt-in flag, same G11 posture as B4. New nullable `AIRun.taskContext` JSON column (server-only read ACL, like `pausedState`) stores the findings + best-effort service name + `code.*` locations at enqueue time — ClickHouse span retention is short, so the evidence must outlive the spans; per-trace dedupe matches the traceId inside it (in-memory scan of the project's rare non-terminal FixPerformance runs — JSON isn't queryable through the Query layer, honestly noted). The claim guard grew a third context kind (`CodeFixTaskTypeHelper.getContextKind`: TelemetryException / IncidentOrAlertSubject / TaskContext) — FixPerformance runs claim with NO exception and NO subject, but Error without a stored traceId. `get-instrumentation-task-details` serves the recipe with `subjectType: "trace"` (rendered evidence rides in `analysisMarkdown`, structured findings + span summaries alongside, so the shared `SubjectPullRequestTaskHandler` pipeline needed zero changes); repository resolution TRIES stack-trace path matching first via a synthetic trace built from the implicated spans' `code.filepath`/`code.function`/`code.lineno` attributes, then the name-match / only-repository fallbacks. New worker handler opens `perf: <finding headline>` PRs (branch `oneuptime-perf-fix-…`) whose body embeds the measured evidence + an explicit review-before-merging note; the prompt casts a performance engineer constrained to fix ONLY the evidenced pattern (batch the N+1, parallelize the sequential siblings, index/cache the dominant operation) — no speculative optimizations. Honest residuals (same as every recipe): **no CI verify** before the non-draft PR; no per-repo PR cap or daily fix-task budget; traces without `code.*` attributes fall back to name-match/only-repo resolution (multi-repo projects without either resolve nothing, run fails with guidance); evidence is single-trace (no cross-trace frequency yet — a preventive-lane W-item candidate). Tests: `SpanTreeAnalyzer.test.ts` (the centerpiece: normalization, all three detectors incl. exact thresholds + self-time semantics + N+1-suppresses-sequential, no-finding traces, ordering, markdown render), `FixPerformanceTaskTrigger.test.ts` (no-finding rejection, repo gate, per-trace dedupe, happy-path taskContext), FixPerformance claim coverage in `AIRunCodeFixClaim.test.ts`. **Migration required:** `AIRun.taskContext` column (entity edited; generate via `npm run generate-postgres-migration`).

- **2026-07-13** — **Shipped Workstream B4: the FixFromIncident recipe — the roadmap's headline `code_fix` (vision §4.8) in human-triggered form.** After a Sentinel investigation completes on an incident/alert, the investigation panel shows one quiet action: **"Open Fix PR from this analysis"**. It calls the new `POST /ai-investigation/create-fix-task` (user auth + subject access check, the panel's existing route idiom), which fail-early gates on: a COMPLETED investigation for the subject (its posted RootCause feed item is the task's entire context), ≥1 GitHub-App-connected repo, and at most one non-terminal FixFromIncident run per subject — then enqueues `AIRun(CodeFix, FixFromIncident)` with `userId` attribution (who clicked). **Human-triggered = no G11 carve-out and no new opt-in flag**: the human reading the analysis before clicking IS the gate, the same posture as the exception-page fix buttons. Plumbing shared with the b3 sibling rather than duplicated: the enqueue idiom (repo check + per-subject dedupe + Queued-run creation) is factored into `SubjectCodeFixRun`, used by both triggers; the recipe-aware claim guard already covered FixFromIncident (grouped by `requiresTelemetryException`); `get-instrumentation-task-details` is generalized to serve any incident/alert-subject recipe (route name kept for agent compat); and the worker's subject-recipe pipeline is factored into `SubjectPullRequestTaskHandler` (sibling of `ExceptionPullRequestTaskHandler`). The new handler opens `fix: <subject title>` PRs (branch `oneuptime-incident-fix-…`) whose body embeds the analysis (6000-char cap) plus an explicit "generated from a Sentinel investigation — review before merging" warning; the prompt casts a senior engineer fixing the identified root cause in a small focused diff following repo conventions, with tests where the repo's conventions make them natural and no unrelated refactors. Opened PRs flow into the `SyncPullRequestStates` outcome instrumentation automatically. Honest residuals (same as every recipe): **no CI verify** before the non-draft PR (needs the code-fix sandbox spike); no per-repo PR cap or daily fix-task budget; trace-less repo resolution means multi-repo projects without a name match resolve nothing (the run fails with guidance); and the button appears under ANY completed investigation — including inconclusive ones (confidence is still regex-over-prose, G6) — relying on the human actually reading the analysis first. Tests: trigger gates (`FixFromIncidentTaskTrigger.test.ts`) + FixFromIncident claim coverage in `AIRunCodeFixClaim.test.ts`.

- **2026-07-13** — **Shipped Workstream B3: the ImproveInstrumentation recipe — an inconclusive investigation becomes the PR that makes the next one diagnosable.** Trigger: when a Sentinel investigation finishes inconclusive (`isConfident=false`), `InstrumentationTaskTrigger` — invoked after the analysis posts, never throwing, so it can never block or fail the investigation — enqueues an `AIRun(CodeFix, ImproveInstrumentation)` carrying the incident/alert subject. Gated per G11 posture on a NEW `Project.enableInstrumentationFixTasks` opt-in (**default FALSE**; toggle on both AI settings pages), on ≥1 GitHub-App-connected repo, and deduped to at most one non-terminal run per subject. The claim guard is now recipe-dependent: exception-based recipes still Error without their exception, instrumentation runs need an incident/alert subject instead, and `exceptionId` became optional on the get-pending-task wire (B1's guard would have errored every such run). New agent-data endpoint `get-instrumentation-task-details` (by run id) returns the subject, the posted analysis (sourced from the RootCause feed item — the only persisted copy written for both subjects and both confidence outcomes), best-effort service name, and the repository resolved **without a stack trace** (name-match / only-repository fallbacks only). New worker handler opens `chore(observability): add instrumentation for …` PRs (branch `oneuptime-instrumentation-…`) whose body embeds the inconclusive analysis as the rationale; the prompt constrains the agent to instrumentation-only diffs in the repo's existing telemetry idioms (no business-logic changes, no new dependencies). Opened PRs flow into the existing `SyncPullRequestStates` outcome instrumentation automatically. Honest residuals: **no CI verify** before the non-draft PR (B4 gap, same as every recipe); no per-repo PR cap or daily fix-task budget (X-lane guardrails still open); trace-less repo resolution means multi-repo projects without a name match resolve nothing (the run fails with guidance). Tests: trigger gates (`InstrumentationTaskTrigger.test.ts`) + recipe-dependent claim guard.

- **2026-07-13** — **Shipped Workstream B2: task-type recipes begin — `codeFixTaskType` discriminator + the regression-test recipe.** `AIRun.codeFixTaskType` (null = FixException) flows through claim → worker dispatch (registry re-keyed by wire string; legacy `AIAgentTaskType` enum retired from the worker) → dashboard labels. First new recipe: **WriteRegressionTest** — a second, independently-deduped action on the exception page that generates a failing test reproducing the production exception (repo conventions, no production-code changes, PR titled `test: reproduce …`, body states it SHOULD FAIL until the bug is fixed). Enum slots declared for the approved next recipes: ImproveInstrumentation, FixPerformance, FixFromIncident (rejected as "not user-triggerable yet" until their triggers ship). Honest residual: nothing verifies the generated test actually fails — that check needs the code-fix track's CI sandbox (B4). Migration `AddCodeFixTaskType…`.

- **2026-07-13** — **Shipped Workstream B1: exception-fix tasks absorbed onto the AIRun substrate.** New runs are `AIRun(runType: CodeFix)` — Queued → CAS-claimed (`claimNextQueuedCodeFixRun`, attemptCount-guarded) → Running with heartbeats → terminal; glass-box trail is typed `AIRunEvent`s (new `ProgressLog` event type carries agent log lines; `ActionExecuted` records the opened PR); duplicate-guard and 3-check readiness unchanged. The agent HTTP protocol kept its routes (get-pending-task now returns `{id, projectId, exceptionId}`; logs land as events; `record-pull-request` links PRs via new `AIAgentTaskPullRequest.aiRunId`). Sweeper semantics: heartbeat-stale CodeFix runs go to **Error, never requeued** (an external executor may have pushed a partial fix branch — G9 fail-closed); orphaned Queued runs fail after 30 min with no alive agent. Legacy `AIAgentTask`/`AIAgentTaskLog`/link tables stop receiving writes (kept read-only for history); the KILL/ABSORB milestone's data-model half is done — the remaining halves are the in-house metered coding sub-agent (B4, needs the sandbox spike) and removing the legacy tables + compose/Helm exemption note. Migration `CodeFixRunsOnAIRun…`. New task types (instrumentation-gap, regression-test, perf, RCA-driven — user-approved; synthetic-script fixer explicitly skipped) will be recipes on this substrate.

- **2026-07-13** — **Shipped zero-config setup part 3: self-host agent-path LLM fallback + agent runs by default.** (1) New `LlmProviderService.getLlmProviderForAgentTasks`: a project-owned provider always wins; when the project owns none AND billing is disabled (self-host), the agent path falls back to the global provider — there it is the operator's own key (usually the `GLOBAL_LLM_PROVIDER_*` env-seeded row), so Workstream A's Cloud metering rationale does not apply; on Cloud (billing enabled) the project-owned-only invariant is unchanged *(superseded 2026-07-13 by B4 Tier 0 for the default agent path: `getLlmProviderForMeteredAgentPath` returns the global provider on cloud too, because completions are now metered server-side; this resolver keeps its restriction for the one-release raw-key fallback only)*. Both agent-path consumers switched (`get-llm-config`, `getAIFixReadiness`), with error/readiness text now pointing self-hosters at the env vars. (2) The AI agent container now runs by default: Helm `aiAgent.enabled` flipped to `true` (key auto-generated in release secrets, agent self-registers, idles when unconfigured) and the production `docker-compose.yml` gains the `ai-agent` service (self-registers via `ONEUPTIME_SECRET`). (3) Docs: `llm-provider.md` gains a zero-config env-var section; `ai-agent.md` prerequisites rewritten for both paths. Boot-time env seeding itself already existed (`SeedGlobalLlmProviderFromEnv` startup migration, every boot) — reused, not duplicated. Tests: fallback matrix in `LlmProviderProjectOwned.test.ts`.

- **2026-07-13** — **Shipped zero-config setup part 2: runtime repository resolution — the `ServiceCodeRepository` mapping table is REMOVED.** Which repo an exception's code lives in is now computed at fix time: stack-trace file paths matched against connected repos' git trees (`StackTraceRepoResolver`, cached via GlobalCache, unique-winner rule — ambiguity never guesses), falling back to exact repository-name matching (`ServiceRepoLinkSuggester`, now runtime-only) and the only-repository rule; every resolution carries human-readable evidence. Consequences, tracked honestly: (1) the **Service Catalog attribution is no longer a fix prerequisite** — Host/infra/unattributed exceptions become fixable when their stack traces match a repo; readiness is now 3 checks, with the resolved repo named as evidence; (2) the mapping table, its dashboard pages, permissions, and the never-consumed guardrail columns (`enableAutomaticImprovements`/`maxOpenPullRequests`/`restrictedImprovementActions`) are dropped (migration `DropServiceCodeRepository…`) — **per-repo fix guardrails must be re-introduced on `CodeRepository` before Preventive-lane X-3 automation** (X-items updated); (3) the agent protocol's `get-code-repositories` now takes `exceptionId` and returns the single resolved repo + evidence (same response shape; in-repo agent updated). PR #2658's suggestion UI was closed unmerged — obsoleted by this change.

- **2026-07-13** — **Shipped zero-config setup part 1: GitHub repos auto-import.** Installing the GitHub App now imports every repository in the installation as `CodeRepository` rows (install-callback import + `installation`/`installation_repositories` webhook sync, both idempotent; removals hard-delete). The per-repo picker modal is gone — connecting GitHub is one click total. Part of the "works from the get-go" direction: the repo-picking step of the four-leg setup is eliminated.

- **2026-07-13** — **Shipped UX-overhaul D2: upfront plan gating + chat empty states.** New `AIPlanGate` banner (Growth-plan check via `SubscriptionPlan.isFeatureAccessibleOnCurrentPlan`, billing-page CTA, renders nothing on self-host/unknown plan — fail open) mounted on AI chat, AI tasks, and Code Repositories pages; the chat home and quick-launch panel now show a "No LLM provider is configured" setup notice with a deep link instead of letting the first message fail at send time. Signposting only — no content blocking.

- **2026-07-13** — **Shipped Preventive-lane X-1 (first half): AI-agent PR outcome sync.** New `AIAgent:SyncPullRequestStates` worker (every 30 min) resolves open `AIAgentTaskPullRequest` rows against GitHub via the App installation (token reused per installation, uninstalled apps skipped) and records Merged / Closed — merged-first mapping so merges never count as plain closes. `pullRequestState` previously stayed "Open" forever, making fix-acceptance rates unknowable; the G11 precision baseline now accumulates from real usage. Merged-with-edits detection and rate reporting remain open.

- **2026-07-13** — **Shipped UX-overhaul C3+D3: AI agent fleet demoted to Settings.** Fleet management (the Probe-pattern AIAgent credential object) moved from AI > Agents to **Settings > AI > AI Agents** at `settings/ai-agents/…` — which makes `AIAgentService.getLinkInDashboard`'s owner-notification links (emails/push/WhatsApp) point at a real page for the first time since they shipped (the server already emitted this path; it was dead in the dashboard). Probe-style "Show ID and Key" added to the list; old `/ai/agents/agents` pages/routes removed; readiness deep-link + guidance strings updated. D3: the AI Logs side-menu items on incident/alert/maintenance views (commented out 2025-12-16, pages orphaned-but-working since) are restored.

- **2026-07-13** — **Shipped UX-overhaul Workstream A** (fail-early + provider hygiene for the legacy fix path): (1) **closed the global-provider leak** — `get-llm-config` now resolves project-owned providers only (`LlmProviderService.getProjectOwnedLlmProvider`); the shared global (OneUptime-billed) provider is never handed to ANY agent, so the unmetered path can no longer spend unbilled global tokens (BYO-key usage remains unmetered until the Phase 3 absorption) *(superseded 2026-07-13 by B4 Tier 0: the default agent path is now server-mediated and METERED, which re-enables the global provider on cloud the right way; this raw-key restriction now governs only the one-release `CODE_AGENT_TYPE=OpenCode` fallback)*; (2) `TelemetryExceptionService.getAIFixReadiness` + `GET /telemetry-exception/ai-fix-readiness/:id` — all four fix prerequisites (project provider, service attribution, GitHub-App repo link, alive agent) checked up front; task creation fails early naming what's missing; the exception page renders a setup checklist with deep links instead of a button that fails minutes later; (3) errored tasks stay visible (latest-task-any-status API + retry); (4) new `AIAgent:FailOrphanedScheduledTasks` sweeper errors tasks no alive agent picked up in 30 min (previously stuck "Scheduled" forever); (5) dead-end token-connect repo flow removed (GitHub App only); (6) Code Repositories moved into the AI nav category + cross-links between the three setup legs; (7) `ai-agent.md` docs cut down to what actually exists. Tests: `TelemetryExceptionAIFixReadiness.test.ts`, `LlmProviderProjectOwned.test.ts`.

- **2026-07-13** — **Added the Preventive lane** (vision §4.14: deterministic silent watchers → Reliability Findings inbox → fix-before-incident PRs), a new track parallel to Phases 2–4 with W/F/X sequencing; new gate **G11** (preventive precision + noise budget); three new non-goals (no LLM in the watch loop; the preventive lane never pages; no auto-merge of AI PRs, ever); vision §6 gains the code-from-telemetry supply-chain threat; Q8–Q10 opened. Substrate audit recorded: `ServiceCodeRepository`'s automatic-improvement guardrail columns (`enableAutomaticImprovements`/`maxOpenPullRequests`/`restrictedImprovementActions`) shipped with zero server-side consumers — and a default-**true** enable flag; the manual fix path has a stack-trace-only context ceiling, no CI verify, and a capless retry; rolling baselines structurally cannot see slow drift (no week-over-week query exists); version changes emit no event (hook-bypassing `serviceVersion` write); marketing claims SLOs the product does not have (Q9).

- **2026-07-11** — The alert re-investigation cooldown (default 30 min, 0 disables, clamp ≤24h) and the concurrent-investigation cap (default 3, clamp 1–25) are now per-project settings (`Project.alertInvestigationDedupeWindowMinutes`, `Project.aiMaxConcurrentInvestigations`) with UI on the AI settings pages; docs table updated.

- **2026-07-10** — G8 preview half: `storeContentPreviews: false` on all five one-shot endpoints and the auto-postmortem path. **Also fixed a metering bypass:** `IncidentService.generatePostmortemFromAI` called `LLMService.getCompletion` directly — auto-postmortems were unmetered, unbilled, and invisible to LlmLog; they now route through `AIService.executeWithLogging`.
- **2026-07-11** — REVERTED `get_service_dependencies` (shipped a day earlier): the underlying service map is not production ready, so exposing it to investigations risked confidently-wrong blast-radius reasoning. The tool survives in git history (13455808bf) for when the map matures — see Deviations D6.
- **2026-07-11** — Queue hardened after adversarial review: (1) claims/transitions now use `AIRunService.attemptStatusTransition` — a single conditional UPDATE — because `updateOneBy` is SELECT-then-save and cannot implement a CAS; (2) the claim also guards on expected `attemptCount`, so attempts can never exceed the max; (3) transient/permanent failure classification is by message (LLMService wraps transient provider errors in `BadDataException` too); (4) `postAnalysis` only fires when the executor WINS the Completed transition (no duplicate RCA if the sweeper falsely requeued a slow run) and heartbeats now touch on every step; (5) the poller claims sequentially but executes detached, keeping the tick inside its job timeout.
- **2026-07-10** — **Phase 2 opened: shipped the durable investigation queue** (`SentinelInvestigationQueue`, Q1 → DB-claim on AIRun rows). Triggers record Queued intent before any expensive work; CAS claims with `attemptCount`; inline kick keeps the 1–3 min latency; every-minute poller drains orphans and expires >30-min queue waits; stale sweeper requeues instead of marking Stale while attempts remain; G9 retry policy (transient requeues, permanent finalizes). D2 residual closed; panel shows the Queued state.
- **2026-07-10** — Shipped the Sentinel user docs page (`/docs/ai/sentinel`): the flagship is no longer dark. Phase 1 checklist is now complete except the measured exit (≥20 production investigations), which starts when the flags go on for our own project. Q3 GTM docs deliverable met.
- **2026-07-10** — Shipped the `baseline_anomaly` read tool: hour-of-week baseline band verdicts (mean ± σ·stddev), cold-start-aware, with an expected-range band chart (first callers for `getBandSeries`/`getCoverage`); the investigation persona now points the model at it.
- **2026-07-10** — RCA now also posted as a bot-authored incident internal note, created with `ignoreHooks` + `isOwnerNotified: true` so the RootCause feed item (quiet-mode gated) stays the single notification source; review caught and reverted an earlier user-less-notes-don't-announce rule that would have silenced SLA note reminders. Also: a daily token limit of 0 now pauses autonomous runs (spend kill-switch) instead of meaning "unlimited".
- **2026-07-10** — Shipped G4 daily budget enforcement: `Project.aiDailyAutonomousTokenLimit` (UTC day, autonomous features only), quiet-skip before run creation, fail-closed backstop in `AIService.executeWithLogging` with `LlmLogStatus.BudgetExceeded`; settings on both AI settings pages. G4 marked MET with residuals noted.
- **2026-07-10** — Shipped alert investigation gating (severity floor defaulting to top two tiers with per-project override; 30-min per-monitor dedupe window via new `AIRun.monitorId`) and the per-project concurrency cap (3) in the shared engine — G4 now partial; Q2 resolved.
- **2026-07-10** — Shipped the alert-side investigation API (`POST /ai-investigation/alert`) + panel; generalized the incident panel into the shared `Sentinel/InvestigationPanel.tsx`; failed investigations now show `errorMessage` detail in the panel (panel half of the Phase 2 G9 surfacing item). Also persisted `cachedInputTokens`/`cacheCreationTokens` to `LlmLog` with AI Logs table visibility.
- **2026-07-10** — Shipped the standalone atomic-claim CAS fix for the legacy `get-pending-task` double-processing race (`AIAgentTaskService.claimNextScheduledTask`); checked off the Phase 1 item and updated the legacy AIAgent table.
- **2026-07-10** — Initial split from `AISentinelReliabilityBrain.md`. Re-baselined all statuses against the tree; added Safety Gates, re-sequenced queue before trigger expansion, pulled topology consumption into Phase 2, split the code-fix track, added GTM lane/packaging, refreshed competitive table (Bits AI GA, incident.io comms, Grafana, Seer, JSM Rovo), corrected the Appendix-era claims (trigger is inline not enqueued; feed-item-only output; two opt-in flags).
