services:
  e2e:
    extends:
      file: ./docker-compose.base.yml
      service: e2e
    image: ghcr.io/oneuptime/e2e:${APP_TAG}
    pull_policy: always
    volumes:
      - ./packages/E2E/playwright-report:/usr/src/app/playwright-report
      - ./packages/E2E/test-results:/usr/src/app/test-results
