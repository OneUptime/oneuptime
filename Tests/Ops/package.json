{
  "name": "@oneuptime/ops-tests",
  "version": "12.0.9",
  "private": true,
  "description": "Static/structural tests for the repo's ops config: ClickHouse config.d drop-ins, docker-compose mounts, and the backup/restore scripts. No services required.",
  "type": "commonjs",
  "scripts": {
    "test": "node ../../node_modules/jest/bin/jest.js --config ./jest.config.json --runInBand"
  }
}
